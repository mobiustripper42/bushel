---
session: 46
dev: eric
slug: main
branch: main
started: 2026-07-06T14:41:51Z
ended:
points:
pr_numbers: [282]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/0abaedfd-afbb-45c3-b29d-ca272e34febc.jsonl
---

# Session 46 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 11.1 — /api/mobile/* routes + bearer-token guard (DEC-047/050/052)

**Completed:**
- bushel's first real HTTP data API for the bushel-mobile Expo app (Server Actions are uncallable from native, DEC-050). closes #261.
- `src/lib/auth/bearer.ts`: `Authorization: Bearer` guard reusing the DEC-047 `verifySession` (no second trust root, timing-safe HMAC compare, expiry check).
- `src/lib/auth/session.ts`: extracted shared `makeSession(subject, now, ttlMs)` so the cookie path and the bearer/API mint from one place; `session-cookie.ts` repointed at it (14-day TTL + sliding renewal unchanged).
- `src/lib/admin/order-mutations.ts`: `advanceOrder`/`fulfillOrder` service core over a private `applyTransition` (one order-row read each). `src/actions/advance-order-status.ts` now delegates — same cookie gate + `revalidatePath` (DEC-052 parity mechanism 1: one write path, two wrappers).
- `src/lib/notifications/push.ts`: `registerPushToken` upsert. `db/migrations/0002_push_tokens.sql`: Expo push-token registry (unique token, no FK per DEC-048) — first migration after the 0001 consolidation.
- Five routes under `src/app/api/mobile/`: `auth/request-code`, `auth/verify-code` (returns the token as JSON for the app to store), `orders` (GET active set), `orders/[id]/fulfill` (POST), `push-tokens` (POST).
- Tests: `bearer.test.ts` (8 unit) + `db/tests/mobile-api.test.ts` (15 pg-integration — auth round-trip, guard, fulfill 200/404/409, push-token upsert, no-enumeration). tsc + lint + `next build` clean; all 5 routes register as server functions.
- `docs/API.md`: the contract for the bushel-mobile repo. `.claude/CLAUDE-context.md`: noted the `/api/mobile/*` Server-Action carve-out.

**Migration applied — Neon `main` (preview base):** `0002` applied via Neon MCP as one transaction (DDL + COMMENT + `_migrations` ledger row, matching `db/migrate.ts` so the runner won't re-apply). Verified: 6 columns, PK + unique, ledger = 0001+0002. **Neon `production` deliberately NOT touched** — held until PR merges + promotion (runbook order). Apply the same transaction (or `db/migrate.ts` explicit-arg) to the `production` branch before `/promote-production`.

**Code review:** No blocking issues. Two notes folded in: (1) `fulfillOrder` double round-trip → single read via private `applyTransition`; (2) CLAUDE-context "Mutations via Server Actions (not API routes)" → amended for the carve-out.
**PR:** [#282](https://github.com/mobiustripper42/bushel/pull/282)
**Points:** 5
**Branch:** task/11.1-api-routes
**Opened at:** 2026-07-06T15:33:35Z

**Next Steps:**

**Context:**
