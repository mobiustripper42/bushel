---
session: 32
dev: eric
slug: main
branch: main
started: 2026-05-25T11:24:30Z
ended:
points:
pr_numbers: [166, 167, 169, 171]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/98434c51-6079-4d4d-b129-518568f1f603.jsonl
---

# Session 32 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Inventory meta pills — derive from schedule, drop Cutoff

**Completed:**
- `src/app/(admin)/admin/inventory/page.tsx` — InventoryPage server component now fans out 5 parallel queries (products, product_units, ordering_schedule singleton, active-customer count, current-week-orders count). Single error gate covers all five responses and a missing singleton row — fixes the silent "Closed · 0 active · 0 ordered" fallback flagged in review.
- `src/components/admin/inventory-editor.tsx` — new `schedule` + `customerStats` props; new `describeSchedule()` helper resolves the 3-state Open-for-orders label (`Closed` / `Open · manual` / `Open · {Day} {time} – {Day} {time}`). Cutoff pill removed entirely.
- `src/lib/schedule-format.ts` — new shared formatters (`DAYS`, `formatTime`, `shortDay`) refactored out of SettingsScheduleCard so the inventory editor can reuse them.
- `src/components/admin/SettingsScheduleCard.tsx` — switched to the shared import.
- `design/admin-inventory.jsx` — mockup mirror (Cutoff pill gone, Open-for-orders updated) per the design-folder-is-authoritative rule.
- `tests/admin-inventory.spec.ts` — new "open-for-orders meta pill" describe block with three cases (each open state) + `afterEach` that restores the singleton row to post-seed default. Existing top assertion now asserts Cutoff is absent rather than present.
- `tests/helpers.ts` — new `setOrderingSchedule()` helper.

**Code review:** Two real bugs caught and addressed in commit `17a22f7`: (1) error short-circuit only checked products + units, so a schedule or count query error silently rendered a state-valid-but-wrong "Closed" pill; (2) missing `ordering_schedule` singleton row would `.single()`-error to the same silent fallback. Both folded into a single `firstError ?? !scheduleRes.data` gate. Non-blocking deferrals: gating-logic dedup with SettingsScheduleCard.stateInfo (extract if a third consumer appears) and moving the test schedule restore into `resetTestData()`.
**PR:** [#166](https://github.com/mobiustripper42/bushel/pull/166)
**Points:** 2
**Branch:** task/6.x-inventory-meta-real-data
**Opened at:** 2026-05-25T12:47:25Z

## Task 2: Consistent "items" count across customer, admin, Telegram (closes #159)

**Completed:**
- `src/lib/order-items.ts` — new shared `totalItemCount(items)` helper. Reduces `Number(it.qty) || 0` across the array (NaN guard for any non-numeric escape) and rounds the result to 2dp so fractional sums under unit-aware multi-unit don't render as `3.3000000000000003 items`.
- `src/components/customer/OrderForm.tsx` — removed the static "X items" availability-header count that never updated based on order state. Submit summary now uses the existing `itemCount` memo (sum of qty) instead of `lineCount` (distinct-line count), matching the sticky bar.
- `src/components/admin/order-row.tsx` — items column uses `totalItemCount(view.items)`.
- `src/lib/notifications/admin-alert-template.ts` + `src/lib/notifications/admin-telegram.ts` — Telegram payload field renamed `lineItemCount` → `itemCount`; body label flipped from "X lines" to "X items" / singular "1 item". `src/actions/place-order.ts` wraps the payload through the helper.
- `design/order-page.{jsx,css}`, `design/admin-orders.jsx`, `design/customer-preview.jsx` — mockup mirrors. Dropped now-orphan `.inv-count` rule from `src/styles/app.css` and `design/order-page.css` (the only consumer was the removed header count).
- `tests/admin-orders.spec.ts:45` — assertion flipped from "2" (line count of kale × 2 + eggs × 1) to "3" (sum); comment references #159.
- `tests/admin-alert-template.spec.ts` — full rewrite for the renamed field + new label.

**Code review:** Four findings, all addressed in commit `fbef714`: (1) `design/customer-preview.jsx` was the lone holdout still showing distinct-line count under an "items" label — exactly the inconsistency #159 set out to remove; (2) `Number(it.qty)` could yield NaN propagation, guarded with `|| 0`; (3) fractional sums rendered with full float precision, helper now rounds to 2dp; (4) `.inv-count` CSS rule orphaned by the header removal, dropped.
**PR:** [#167](https://github.com/mobiustripper42/bushel/pull/167)
**Points:** 2
**Branch:** task/6.x-consistent-item-count
**Opened at:** 2026-05-25T15:34:08Z

## Task 3: Drag-to-reorder inventory rows (closes #143)

**Completed:**
- `src/components/admin/inventory-editor.tsx` — `draggingId`/`dropTargetId` state and `reorderRows(fromId, toId)` that splices the source to land before the target then rewrites sort_order to (10, 20, 30, …). Defensive `setDraggingId(null)/setDropTargetId(null)` at the top of reorderRows so a dropped `dragend` event (cross-window drops, some WebView edge cases) can't strand a row at 40% opacity.
- `src/components/admin/inventory-row.tsx` — "armed handle" pattern: `<tr draggable={dragArmed}>` only after `onMouseDown` on the grip; `onDragEnd` disarms. Stops field clicks from starting an accidental drag. Native HTML5 DnD throughout — no new dependency.
- `src/styles/app.css` + `design/admin-inventory.css` — `.is-dragging` (40% opacity) + `.is-drop-target` (2px top border in `var(--leaf-600)`).
- `design/admin-inventory.jsx` — mockup mirror (parent state + per-row armed handle + drag handlers).
- `tests/admin-inventory.spec.ts` — drags Honey above Kale via Playwright's `dragTo()`, asserts DOM order + dirty-count badge ("Save 3 changes" — every row's sort_order shifted) + Save + reload persistence. Try/finally wraps the body with new `resetProductSortOrder()` helper so a mid-test failure can't leak ordering to downstream specs.
- `tests/helpers.ts` — new `resetProductSortOrder()` helper.

**Code review:** Five findings, two real cleanups addressed in commit `36eddf9`: (1) defensive draggingId clear; (2) Firefox-narrow setData comment; (3) try/finally restore in spec; (4) inline test comment on the dirty-count surprise. One redundancy dropped (toIdx pre-check). Two follow-up items filed as issue #168: drop precision (above/below by cursor Y) + tightening "Save N changes" copy for reorder-only diffs.
**PR:** [#169](https://github.com/mobiustripper42/bushel/pull/169)
**Points:** 3
**Branch:** task/7.x-inventory-drag-reorder
**Opened at:** 2026-05-25T16:04:35Z

## Task 4: Admin customers — show + reactivate deactivated rows (closes #61)

**Completed:**
- `src/app/(admin)/admin/customers/page.tsx` — dropped the `is_active=true` filter from the query, included the flag, added `order("is_active", { ascending: false })` so active rows naturally lead.
- `src/components/admin/customers-page.tsx` — `showDeactivated` state, header toggle button (only renders when `deactivatedCount > 0` and disappears after the last reactivation), `is-inactive` row class, `data-row-state` test hook, Subscribed switch disabled on inactive rows. Eyebrow stays "{activeCount} accounts · {subscribedCount} subscribed" (active counts only — the toggle text carries the deactivated number separately). Empty-state copy branches on `showDeactivated` so "No active customers" doesn't lie when the deactivated view is empty.
- `src/components/admin/customer-drawer.tsx` — new `is_active` in `CustomerDrawerState`; `isInactive` derived from `initial.is_active` (snapshot at open — drawer closes on reactivate so reopen always gets a fresh prop); eyebrow swaps to "deactivated customer"; Delete button hidden when inactive, Reactivate button shown instead, wired through new `handleReactivate` transition with symmetric disabled prop (`reactivating || saving || deleting`).
- `src/actions/reactivate-customer.ts` — new server action: flips `is_active=true`, revalidates `/admin/customers` + `/admin/inventory` (subscribed-pill consumer per session-32 #159 work).
- `src/components/ui/switch.tsx` — gained `disabled?: boolean` prop; passes through to button + adds `.is-disabled` class.
- `src/styles/app.css` — `.cust-row.is-inactive` (diagonal-stripe background + strike-through name, visually distinct from `.is-unsub`'s solid muted background) + `.switch.is-disabled`. Mirrored in `design/admin-customers.{jsx,css}`.
- `tests/admin-customers.spec.ts` — new spec deactivates restaurant fixture via admin client, asserts hidden by default + eyebrow drops to "1 account", toggle reveals with is-inactive + disabled switch, drawer shows Reactivate (not Delete), reactivate restores row + makes toggle disappear.

**Code review:** Four findings, all minor cleanups; three addressed in commit `fad12b1` (Reactivate-button disabled symmetry, empty-state branch on `showDeactivated`, test comment "afterEach" → "beforeEach"). Eyebrow-when-showDeactivated copy deferred per agent's "not blocking for a 7-customer tool" note. Sound-by-design items confirmed: isInactive snapshot, multi-key sort within is_active groups, CSS specificity of is-inactive vs is-unsub, save-while-deactivated preserves the flag, test fixture cleanup via beforeEach.
**PR:** [#171](https://github.com/mobiustripper42/bushel/pull/171)
**Points:** 2
**Branch:** task/7.x-show-deactivated-customers
**Opened at:** 2026-05-25T16:35:02Z

**Next Steps:**

**Context:**
