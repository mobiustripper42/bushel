---
session: 14
dev: eric
slug: main
branch: main
started: 2026-05-11T13:09:53Z
ended: 2026-05-12T00:36:36Z
duration: 11.5
points: 9
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/c98e856d-6c8f-4707-88ab-501150fddf66.jsonl
---

# Session 14 — main

**Task:** Phase 2 close-out (2.1 + 2.2) and Phase 3 kick-off (3.0 + 3.1) — long session, four PRs cut, two that need redo against the design folder.

**Completed:**
- **Task 2.1** (5 pts, merged to main via PR #44) — `/admin/inventory` editor (Server Action + Client `InventoryRow` + `playwright.config.ts` dotenv loading + test products in seed/global-setup). ⚠ **Does not match `design/admin-inventory.jsx`** — needs full rebuild.
- **Task 2.2** (2 pts, PR #56 open ready-to-merge) — `prepopulate_inventory_from_last_week()` Postgres function + Server Action + button + 2 Playwright tests. Derives from `order_items` history per DEC-012. ⚠ Button placement on inventory page will move during 2.1 redo.
- **Task 3.0** (2 pts, merged to main via PR #54) — `customers.priority integer not null default 100 check (priority >= 0)` migration for DEC-026/027 send-queue ordering. pgTAP grew 43 → 46.
- **Task 3.1** (3 pts, orphaned in `task/3.0` branch) — `/admin/customers` CRUD with email/phone OR-validation + active toggle + 5 Playwright tests. ⚠ **Does not match `design/admin-customers.jsx`** — discard, rebuild from scratch.
- **Phase 3 materialized** via `/start-phase 3` — Issues #45–#53 (34 pts) with `phase:3` + `points:N` labels; `docs/PROJECT_PLAN.md` updated with links.
- **CLAUDE.md hardened** — added "READ FIRST: design folder is authoritative" header + the `source .envrc && supabase db push` one-liner. Both on main.
- Remote Supabase caught up: pushed 2.0a + 2.0b migrations that were drifting (`add_product_category`, `replace_notification_preference`, `fulfillment_schema_drop_pickup_windows`).
- closed #32 (2.1), #33 (2.2), #45 (3.0), #46 (3.1, though 3.1 will be reopened/redone).

**In Progress:** PR #56 awaiting merge. Two redo tasks identified but not started.

**Blocked:** nothing technical.

**Next Steps (cold-start ready):**
1. **Merge PR #56** (2.2 prepopulate clean cherry-pick to main).
2. **Push remaining migrations to remote:** `source .envrc && supabase db push` — applies `add_customer_priority` + `prepopulate_inventory_from_last_week` function on the cloud DB.
3. **Rebuild 2.1 against design** — `design/admin-inventory.jsx` + `design/admin-inventory.css`. Key differences vs what shipped: one "Save N changes" button at top-right (dirty-aware), dirty-state save bar at bottom, category dropdown per row, collapsible description field, drag handle, toggle switch (not checkbox), trash + Add row, meta pills (Open / Customers / Cutoff), "Week of …" in title. Re-place the existing `<PrepopulateButton />` as a secondary top-right action per the mockup.
4. **Rebuild 3.1 against design** — `design/admin-customers.jsx`. Don't open it cold without reading first.
5. Optional housekeeping: `git push origin --delete task/2.1-inventory-editor task/3.0-customer-priority-column task/3.1-admin-customer-crud task/2.2-prepopulate-from-last-week task/2.2-prepopulate-clean` after #56 merges.
6. After redos, continue Phase 3 from **task 3.2** (#47, 5 pts) — proper crypto token generation + regenerate button. Carry-forward: regenerate must be mandatory before `send_weekly_link` fires, so the `placeholder-*` tokens never reach a customer URL.

**Context:**
- **Design folder was missed for the third time.** `design/admin-inventory.jsx`, `design/admin-customers.jsx`, etc. are the authoritative UI spec. CLAUDE.md now has a "READ FIRST" header making this impossible to overlook — but if it's missed again, the rule has failed and needs a different mechanism (hook? agent enforcement?).
- **Stacked-PR gotcha:** GitHub doesn't auto-rebase stacked PR bases when the parent merges. PRs #43 and #55 merged into `task/2.1` and `task/3.0` respectively, not main, leaving orphaned work on those branches. Recovered 2.2 via a clean cherry-pick branch (PR #56); 3.1 will be redone so its orphan can be deleted.
- **`.env.local` env swaps mid-session:** remote → local for testing migrations (`supabase status --output env`), then back to remote so user could review via Google OAuth. Remote keys preserved as commented block in `.env.local`.
- **DEC-012 enables snapshot-free pre-populate:** orders decrement `qty_available` (allow negative), so "last week's starting qty" = current + SUM(last week's `order_items.qty`). No snapshot table needed — pure computation.
- **`settings.local.json` auto-pollution recurred** (same as session 13) — picked up an allowlist entry with the Supabase access token embedded mid-session. Reverted before commit. Recurring noise; may warrant a `git update-index --skip-worktree` or similar.
- **Playwright `revalidatePath` race on customer-form save:** the "Saved" inline indicator races the Server Component re-render in the customer-form Client Component. Worked around by asserting `toBeEnabled()` (settles when `useTransition`'s pending resolves) + a direct DB read for canonical truth. Will recur in 3.1 redo unless `useActionState` pattern is adopted.
- **Test customer Farm Stand has no email/phone in `seed.sql`** — the new OR-validation rejects updates without contact info. Edit-flow Playwright test fills an email before saving and `afterEach` reverts.
- **`.claude/scheduled_tasks.lock`** added to `.gitignore` (was showing in `git status` after ScheduleWakeup calls).
- **Duration note:** 11.5h raw elapsed includes a ~4h away-from-desk gap (20:09 → 00:23 UTC, switched to phone). Active working time was closer to ~7.5h. Velocity calculation should weight accordingly — 9pts / 7.5h = 0.83 hr/pt active, vs 9pts / 11.5h = 1.28 hr/pt elapsed.
- **Points note:** 9 pts shipping (5 + 2 + 2 for 2.1 + 2.2 + 3.0). 3 pts of work on 3.1 done but discarded (wrong-design), not counted.

**Code Review:**
- 2.1: missing `revalidatePath` + price wording bug ("positive number" allowed 0). Both fixed pre-merge.
- 2.2: doc nit on RLS-policy comment in migration. Fixed pre-merge.
- 3.0: missing CHECK constraint on priority. Fixed pre-merge (`check (priority >= 0)`).
- 3.1: clean bill of health from @code-review — but **the whole PR doesn't match the design**, which the reviewer wasn't told to check against. Carry-forward for 3.1 redo + 3.2: regenerate-token must be mandatory before `send_weekly_link` fires so `placeholder-*` tokens never reach customer URLs.
