---
session: 21
dev: eric
slug: main
branch: main
started: 2026-05-14T15:28:20Z
ended:
points:
pr_numbers: [76, 77, 78]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/b2960785-7f3a-4695-bbe5-ba6514338f26.jsonl
---

# Session 21 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Split Supabase into dev/preview + prod projects

**Completed:**
- New prod Supabase project provisioned (`piaobrnrmoxnfrpnpixw`); existing project (`nnmfubmlvnkouxxfxxlh`) renamed to "bushel-preview" and retained for `.env.local` + Vercel Preview/Development.
- 8 migrations pushed to prod via `supabase link --project-ref piaobrnrmoxnfrpnpixw && supabase db push`; relinked back to dev immediately after.
- Vercel Production env vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) re-scoped to the prod project; Preview/Development untouched.
- `main` redeployed on Vercel; `order.baybranchfarm.com` homepage + `/login` both load against the new prod DB.
- Prod Supabase auth configured: Site URL = `https://order.baybranchfarm.com`, Redirect URLs include `https://order.baybranchfarm.com/**` (double-star), Google OAuth provider enabled with existing client (added new callback URI to Google Cloud Console), email provider disabled.
- Admin rows seeded for `eric@stoffer.net` and `annabel@stoffer.net` after both completed first Google sign-in.
- End-to-end verified: Annabel can add customers and inventory in prod.
- `CLAUDE.md` updated (this PR): new "Two Supabase projects" section with the project-ref table + migration discipline (link to dev by default, temp-link to prod for pushes, always relink); rewrote "Cross-system env-var sync" to reflect intentionally-different Production vs Preview values + new "swapped projects" failure mode; removed the unused `safe-supabase.sh` wrapper docs (user opted out — "if I accidentally blow it all away it's not the end of the world").
- Stale `origin/claude/fix-bushel-timing-B1dYx` branch deleted (zero commits ahead of main).

**Code review:** Three findings; two folded in pre-PR (drop orphan wrapper mention, hoist `source .envrc` note). Third (optional cross-reference) skipped — would be churn.
**PR:** [#76](https://github.com/mobiustripper42/bushel/pull/76)
**Points:** 3 (infra + docs, no GitHub issue — opportunistic, retroactively a Phase 2 retro miss per user)
**Branch:** task/infra-split-supabase-projects
**Opened at:** 2026-05-14T16:25:00Z

## Task 2: CI — run Playwright tests inside the Playwright Docker container (#65)

**Completed:**
- `.github/workflows/ci.yml` — added `container:` block (`mcr.microsoft.com/playwright:v1.59.1-noble`) so browsers + apt deps are baked in. Targets dropping the per-PR install step from ~6 min cold to ~10–30 sec image pull (AC: under 60 sec).
- `container.options` carries both `--add-host=host.docker.internal:host-gateway` (so the Next.js app inside the container can reach Supabase containers on the host network) and `-v /var/run/docker.sock:/var/run/docker.sock` (so the Supabase CLI can shell out to the host daemon).
- Added an `Install Docker CLI` step that drops the static `docker` binary into `/usr/local/bin/` (~10s) since the Playwright image doesn't ship one.
- Dropped `actions/setup-node` — noble image ships Node 22; `setup-node`'s `cache: npm` misbehaves on container `$HOME` paths.
- Rewrote `Set Supabase env vars` step to substitute **both** `127.0.0.1` and `localhost` → `host.docker.internal` in `API_URL` (Supabase CLI version may emit either spelling).
- `package.json` — pinned `@playwright/test` from `^1.52.0` to exact `1.59.1` so npm-resolved version can't drift past the image tag on a future install.
- `package-lock.json` regenerated by `npm install` (no actual version change — was already on 1.59.1).
- Local `npm run build` green.

**Code review:** First pass caught a real bug (Supabase CLI couldn't reach docker daemon inside container — GHA doesn't auto-mount the socket and Playwright image has no docker CLI). Folded Path A response pre-PR: socket mount + static docker CLI install + drop setup-node + broader URL rewrite. Workflow self-validates on first CI run on this PR.
**PR:** [#77](https://github.com/mobiustripper42/bushel/pull/77)
**Points:** 5 (no estimate in #65 itself, but the topology investigation + review iteration justify it over a 3)
**Branch:** task/infra-ci-playwright-container
**Opened at:** 2026-05-14T16:55:00Z

**Sailbook comparison (for the curious):** sailbook's `playwright.yml` runs the same uncached `npx playwright install --with-deps chromium` and is just as slow per-job, but uses a 3-way matrix (desktop/tablet/mobile) so wall-clock is masked. Bushel runs serially in one job, so the 6 min sticks out.

## Task 3: Phase 3.5 — optimistic order placement (#50)

**Completed:**
- New migration `supabase/migrations/20260514173354_place_order_function.sql` — plpgsql RPC `place_order(...)`. `security invoker` + pinned `search_path`. `INSERT ... ON CONFLICT (customer_id, week_of) DO NOTHING RETURNING` for atomic double-submit (replaced naive select-then-insert per code review — TOCTOU on fast double-tap). Decrements `products.qty_available` per line, allowed to go negative (DEC-012). Computes `needs_reconciliation` at the end via a single set query. `service_role`-only execute grant.
- New `src/actions/place-order.ts` — looks up customer by `bbf_customer_token` cookie, calls the RPC via the admin client, redirects to `/c/[token]/confirmed` on success. Returns `{ error }` on validation failures.
- `src/components/customer/OrderForm.tsx` — all three submit buttons (sticky-bar, bottom-block, rail-card) wired to one handler under `useTransition` + a synchronous `useRef` latch (the latch closes a cross-button-tap race that `useTransition`'s isPending doesn't catch). In-button spinner during pending; `aria-busy` + `disabled`. Inline error display under the bottom block.
- `src/app/c/[token]/confirmed/page.tsx` — placeholder swapped for real lookup via `getCurrentWeekOrder`. Cold-navigation case shows a "no order yet" state with a link back to `/c/[token]`.
- `src/lib/customer/queries.ts` — new `getCurrentWeekOrder` helper joining orders + order_items + product name/unit. Added a header comment documenting the service-role-as-gate pattern (the customer-side bypasses RLS intentionally; the token cookie is the boundary).
- `src/lib/week.ts` — new `weekOfMondayNY()` helper. Sunday maps to *next* Monday (matches the Sun/Mon → Wed/Thu cadence). Code review caught my original code mapping Sunday → last Monday, which would silently bucket Sunday-evening orders into the prior week and (combined with the unique constraint) redirect customers to last week's `/confirmed`.
- `src/styles/app.css` — `.btn-spinner` keyframe primitive + `.submit-error` block. Both reuse existing tokens.
- `src/lib/supabase/types.ts` — regenerated for the new RPC.
- New `tests/customer-place-order.spec.ts` — happy / oversold / double-submit scenarios. All 3 pass on desktop + tablet + mobile.

**Code review:** Three real findings, all folded in pre-PR on `0fc32dd`: (a) weekOfMondayNY Sunday mapping was wrong, (b) TOCTOU race in place_order, (c) useTransition-only double-submit guard had a cross-button race. Plus hardening: `security invoker set search_path`, `p_items` cast to `Json`, header comment on `queries.ts`. One reviewer follow-up deferred: `/confirmed` queries by current-week, so a week-boundary visit won't surface last week's order — small follow-up issue if it becomes a problem.
**PR:** [#78](https://github.com/mobiustripper42/bushel/pull/78)
**Points:** 3 (issue estimate held)
**Branch:** task/3.5-place-order
**Opened at:** 2026-05-14T17:50:00Z

**Next Steps:**

**Context:**
