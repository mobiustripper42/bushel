---
session: 41
dev: eric
slug: main
branch: main
started: 2026-07-01T21:23:45Z
ended:
points:
pr_numbers: [239, 240, 242, 243, 244, 247, 249]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/82559c0b-4196-4ca7-b20b-bd66e0043f7c.jsonl
---

# Session 41 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Bundle A — open-order pivot core (9.2 + 9.5 + 9.3a + 9.3b)

**Completed:**
- **9.2 (#225, DEC-040):** removed the `crons` entry from `vercel.json`; check-schedule route + `SettingsScheduleCard` marked dormant in comments, machinery left in place.
- **9.5 (#229, DEC-044):** `picked-up` → `picked_up` across `orders-queries.ts` (ORDER_STATUSES, guards, narrowStatus), `order-actions.tsx`, `order-row.tsx`, `report.ts`, and specs/helpers.
- **9.3a (#226, DEC-041):** `supabase/migrations/20260701213000_open_order_identity.sql` — cutover truncate of orders/order_items/customer_sends IN the migration, drop `unique (customer_id, week_of)`, partial unique index `orders_one_open_per_customer`, `CREATE OR REPLACE place_order`: replay join customer-scoped, ON CONFLICT infers the partial index, FOR UPDATE keyed to open status, create-or-find retry loop closes the insert-vs-lock terminal race. Terminal refusal now structural (fulfilled frees a new order). pgTAP rewritten: 32 tests.
- **9.3b (#227, DEC-042):** `getOpenOrder`/`getLatestOrder` replace `getCurrentWeekOrder` (`src/lib/customer/queries.ts`); `/c/[token]` renders the open order pre-populated in add mode (bounce + `?add=1` gone); `/confirmed` keys to latest order, terminal read-only + "Place a new order"; `report.ts` → `listActiveOrders()` (all open orders, any week); `listOrders` refactored to `queryOrders`, sends map re-keyed `(customer_id, week_of)`; `tests/global-setup.ts` prior-order fixture switched from upsert-on-dropped-constraint to check-then-insert.
- New model choices flagged + approved in-flight: truncate inside migration; `?add=1` removed; terminal receipt offers "Place a new order".

**Code review:** @code-review — 1 real catch (closed/sold-out shells hid the open order → redirect to /confirmed + regression test), 2 minor fixed (receipt week label from `order.week_of`; pgTAP `appended` assert on terminal replay). Skipped: inert schedule-card copy (DEC-040 accepted), admin week-filter gap (= 9.8), sends-fetch parallelism (9.8 rewrites path).
**PR:** [#239](https://github.com/mobiustripper42/bushel/pull/239)
**Points:** 11 (1 + 2 + 5 + 3)
**Branch:** task/9.x-open-order-core
**Opened at:** 2026-07-01T22:10:00Z

## Task 2: Admin orders — action stack into the detail grid (alignment fix)

**Completed:**
- Eric's ask: expanded row had dead space (action stack in the status cell stretched the row; detail panel floated below). Stack now renders as `ord-detail-grid`'s third column (`1.2fr 1fr 186px`) — "Line items" tops out level with "Confirm order". `src/components/admin/order-row.tsx`, `order-detail.tsx` (new `actions` prop), `src/styles/app.css`.
- Mobile cards float actions to the top of the detail panel (`order: -1`), touch targets preserved.
- Detail `tr` gains `data-order-id`; spec locators re-scoped (`detailRowFor`) in `admin-orders.spec.ts` + `orders-flow.spec.ts`.

**Code review:** @code-review — 1 behavioral catch fixed (advance error was hidden once the row collapsed → moved back to the always-visible status cell); vacuous collapsed-state assertion tightened; stale comment fixed.
**PR:** [#240](https://github.com/mobiustripper42/bushel/pull/240) — **stacked on #239**, merge that first
**Points:** 1
**Branch:** task/9.x-orders-actions-alignment
**Opened at:** 2026-07-01T23:35:00Z

## Task 3: #241 — consolidate duplicate order lines (Eric's preview feedback)

**Completed:**
- Found by Eric testing #239: DEC-039 appends insert one `order_items` row per submission, so a twice-topped-up product rendered as 3 lines. Filed as issue #241 (net-new, not 9.4/9.6/9.8).
- `consolidateItems()` + shared `consolidationKey(product, unitLabel, price)` in `src/lib/order-items.ts`; price in the key keeps mid-week price changes as separate invoice lines. DB rows stay granular (submission_id audit/idempotency).
- Applied once in `queryOrders` (covers admin detail, items preview, packing slips, Wave export) + `/confirmed` receipt + add-mode "Already ordered" group (`CUSTOMER_ORDER_SELECT` now carries `product_id`).
- `order-detail.tsx` line key widened to product+unit+price (fixes pre-existing dup-React-key).
- New tests: same-product append → 1 line on receipt/add-mode/admin detail, DB keeps 2 granular rows.

**Code review:** @code-review — clean on correctness (label proven faithful proxy for product_unit_id; export/report verified). Applied: shared consolidationKey. Deferred: per-product oversold summing when a product spans lines (price change) — rare, noted in PR.
**PR:** [#242](https://github.com/mobiustripper42/bushel/pull/242) — stacked on #240 → #239, merge in order
**Points:** 2
**Branch:** task/9.x-consolidate-lines
**Opened at:** 2026-07-02T00:20:00Z

## Task 4: 9.8 (#231) — admin orders Active/Fulfilled views (DEC-045)

**Completed:**
- `/admin/orders` drops the week filter: **Active** (non-terminal, any week, default) / **Fulfilled** (browsable past) chips, `?view=fulfilled`. Cross-week open orders no longer vanish at the boundary.
- Week-keyed `listOrders` deleted; `listActiveOrders`/`listFulfilledOrders` + `countOrdersByStatus` (head-only count for the hidden tab) in `src/lib/admin/orders-queries.ts`. `TERMINAL_ORDER_STATUSES` single source (isTerminalStatus + filter derive from it).
- Export ships the on-screen view; filenames `bushel-orders-<view>-<week>.csv` (ExportOrdersButton `weekOf` → `filenameLabel`).
- "Custom range" dead stub removed; per-view empty copy. Sends-join 1:1 fix was already done in #239.
- Tests: new cross-week visibility test; delivered-order assertions moved to `?view=fulfilled`; export test reworked to view semantics; global-setup fixture documented as deliberately itemless (export spec depends on it).

**Code review:** @code-review — all four findings applied (Active export filename mislabel, terminal-set triplication, unbounded hidden-tab fetch, undocumented fixture dependency). Deferred: status-keyed test-reset helper (revisit on first flake).
**PR:** [#243](https://github.com/mobiustripper42/bushel/pull/243) — based on main
**Points:** 3
**Branch:** task/9.8-admin-orders-active-view
**Opened at:** 2026-07-02T01:25:00Z

## Task 5: Bundle B — 9.4 per-unit SKU (#228, DEC-043) + 9.6 hidden-state visual (#230)

**Completed:**
- **9.4:** migration `20260702100000_product_units_sku.sql` (nullable `sku`, no backfill); units drawer SKU column (6-col grid desktop, full-width wrap ≤480px, `app.css`); `save-product-units` writes trimmed-or-null; Wave export Item Number = line's unit `sku || slug || ""` — `products.description` out of the export, back to customer-facing-only. `OrderItem` swaps `description` for `sku`/`slug`; Supabase types regenerated. Specs: sku-save assertion, slug-fallback expectations (incl. orders-flow kale line → `kale-cccccccc`), mock updates.
- **9.6:** `.pill-hidden` "Hidden" pill in the name cell + rose inset accent bar (`td:first-child` desktop, card edge ≤640px since that td is hidden there), hatch darkened `--ink-100` → `--ink-200`. `.is-disabled` untouched.

**Code review:** @code-review — chain verified end-to-end incl. consolidated lines; applied mobile accent-bar fix + `||` fallback for stray empty-string sku. Skipped: mobile SKU micro-label (aria-label covers AT).
**PR:** [#244](https://github.com/mobiustripper42/bushel/pull/244) — **stacked on #243**, merge in order
**Points:** 5 (3 + 2)
**Branch:** task/9.x-bundle-b
**Opened at:** 2026-07-02T02:30:00Z

## Task 6: 9.7 (#235) — harvest sheet print readability

**Completed:**
- Pick list: CSS masonry (`columns: 2`) → real row-major 2-col grid; each row is `name | aligned qty column` (receipt read). `src/styles/app.css`, tick-box span in `fulfillment-report.tsx`.
- Print block: 12pt/11pt near-black type (ink tokens), print-only pencil tick box per product, `@page { margin: 14mm }` (app-wide, documented deliberate), `break-inside: avoid` on slips.
- Mobile single-column preserved (`grid-template-columns: 1fr`).
- Verified via print-media-emulated screenshot at US Letter; real-laser print is Eric/Annabel's acceptance.

**Code review:** @code-review — clean (overflow guards, a11y, token darkness, spec independence verified); applied a comment documenting the app-wide @page scope.
**PR:** [#247](https://github.com/mobiustripper42/bushel/pull/247) — based on main
**Points:** 2
**Branch:** task/9.7-harvest-print
**Opened at:** 2026-07-02T03:25:00Z

## Task 7: CI typecheck + lint gates (#238 follow-up)

**Completed:**
- `ci.yml`: `npx tsc --noEmit` + `npm run lint` steps after `npm ci`, before the build — closes the "pending Eric's call" follow-up from Session 40. Verified green locally (one standing aria-sort warning doesn't gate).
- Issue-less chore, Eric's direct ask.

**Code review:** skipped — 9-line CI-only diff; the PR's own CI run is the verification.
**PR:** [#249](https://github.com/mobiustripper42/bushel/pull/249)
**Points:** 1
**Branch:** chore/ci-typecheck-lint
**Opened at:** 2026-07-02T03:50:00Z

**Next Steps:**

**Context:**
