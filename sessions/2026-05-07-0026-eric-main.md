---
session: 4
dev: eric
slug: main
branch: main
started: 2026-05-07T00:26:15Z
ended: 2026-05-07T01:38:10Z
duration: 1.2
points: 5
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/9728a2b7-2ac2-4033-bf2e-007d24b79948.jsonl
---

# Session 4 — main

**Task:** Phase 0.6 — GitHub Actions CI (closes #6) + rename `PUBLISHABLE_KEY` → `ANON_KEY`

**Completed:**
- Renamed `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` → `NEXT_PUBLIC_SUPABASE_ANON_KEY` in `src/lib/supabase/{client,server,middleware}.ts`, `.env.example`, `.env.local`
- Wrote `.github/workflows/ci.yml`: Supabase CLI 2.98.2 → `supabase start` → pgTAP → `npm ci` → Playwright browsers → set env vars → build → `nohup npm start -- -p 3001` → wait-for-port curl → `npx playwright test` → artifact upload on failure
- Fixed 4 code-review bugs before PR open: wrong port, fragile backgrounding, silent empty env vars, missing `NOTIFICATIONS_ENABLED` on app process; pinned CLI version
- Dropped redundant `push:` trigger — CI runs once per PR only
- PR #13 merged, closes #6

**In Progress:** nothing

**Blocked:** nothing

**Next Steps:**
1. Confirm `.env.local` uses `NEXT_PUBLIC_SUPABASE_ANON_KEY` on any other machines
2. Next task: Phase 0.7 (Issue #7, 3 pts) — BRAND.md + ui-reviewer customization

**Context:**
- `supabase status --output env` uses `API_URL` (not `NEXT_PUBLIC_SUPABASE_URL`) and `ANON_KEY` — the step maps them before build
- Supabase CLI pinned at `2.98.2` to match mill-dev; update deliberately
- `nohup npm start -- -p 3001 & disown` is the reliable pattern for backgrounding on GHA runners
- `pull_request:` trigger only — no `push:` needed since CI runs on every PR push and merge just lands on main

**Code Review:** 4 bugs fixed before open (port, backgrounding, empty env guard, NOTIFICATIONS_ENABLED gap); CLI float pin fixed. Clean after fixes.
