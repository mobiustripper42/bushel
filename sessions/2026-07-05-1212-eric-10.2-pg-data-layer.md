---
session: 45
dev: eric
slug: 10.2-pg-data-layer
branch: task/10.2-pg-data-layer
started: 2026-07-05T12:12:13Z
ended:
points:
pr_numbers: [271, 273, 274]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/1a486546-1b51-4455-8d9e-f878299d45f2.jsonl
---

# Session 45 — 10.2-pg-data-layer

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 10.3 — Email-code admin auth (muster session.ts + login_codes + Resend, DEC-047)

**Completed:**
- Dropped Google OAuth / Supabase Auth for a self-rolled email→code→HMAC-session flow (DEC-047, supersedes DEC-003). closes #256.
- `db/migrations/0002_login_codes.sql`: `admins` allowlist (Emma/Eric/Annabel) + `login_codes` (muster 0012 port; `timestamptz` not `text` to match the pg baseline; no FKs per DEC-048). Test admin upserted by global-setup with a fixed id, NOT seeded — keeps `test-admin@bushel.test` out of prod.
- New `src/lib/auth/`: `session.ts` (pure node:crypto HMAC), `config.ts` (cookie/secret/TTLs, no next/headers so proxy imports it), `session-cookie.ts` (glue + sliding renewal; `secure` gated on `x-forwarded-proto` for the WebKit-over-http CI trap), `login-code.ts` (request/verify on direct pg — no-enumeration, 5-attempt cap, 10-min TTL, 60s cooldown, CAS consume), `email.ts` (Resend via fetch, no SDK).
- `getAdminUser` → HMAC session (14 call sites unchanged); `proxy.ts` verifies the cookie for `/admin` (Next 16 Node-runtime proxy); two-step login page; session-based sign-out + admin layout.
- Removed OAuth callback route + dead supabase middleware helper (other `@supabase` files now fully dead — 10.5 removes).
- Tests: global-setup mints the session directly (no supabase/inbox); `admin-auth.spec` drives the real request→verify flow (8 specs). CI gets a fixed `SESSION_SECRET`; `.env.example` documents the new keys.
- Verified locally against docker pg: build, tsc, lint, 8 auth specs + admin-action specs (getAdminUser attribution), WebKit 375px (admin session cookie over http).

**Code review:** Clean on all security-critical pieces (HMAC verify, CAS consume, attempt cap, no-enumeration, timing-oracle closed via `after()`). Fixed: `.env.example` now documents `SESSION_SECRET`/`RESEND_*`. Noted on #257 (10.4): `sent_by_user_id` id-space already flipped to `admins.id` for new writes (inert today — write-only, no FK).
**PR:** [#271](https://github.com/mobiustripper42/bushel/pull/271)
**Points:** 8
**Branch:** task/10.3-email-code-auth
**Opened at:** 2026-07-05T13:30:00Z

## Task 2: 10.4b — Adopt vitest test stack; migrate DB-invariant + auth-logic tests off pgTAP (DEC-051)

**Completed:**
- Rescope decision (Eric): pgTAP-on-docker was a hack; greenfield = keep the concurrency-critical logic in plpgsql, test it through the app's pg driver with **vitest**. Split #257 (10.4) → schema-only, **1 pt**; new **#272 (10.4b)**, 5 pts, is this task. New **DEC-051** + amends DEC-048.
- `vitest.config.ts` (`@/` alias, `fileParallelism: false` — integration files share one DB + truncate), `package.json` test:unit/test:watch.
- `db/tests/helpers.ts` — canConnect (skip cleanly if no DB), migrateTestDb, resetData, closePool. Integration tests drive plpgsql through `src/lib/db` against docker pg — no extension, no pg_prove.
- Ported all 6 function/data pgTAP → `db/tests/*.test.ts` via 4 parallel subagents on isolated DBs (bushel_test_a–d): place-order-additive (32 asserts), place-order-units (19), set-base-unit (17), product-units, order-status-codes, customer-sends-modes. RLS-only assertions dropped (DEC-048); the 3 RLS-only pgTAP files + all `supabase/tests/` deleted.
- Backfilled 10.3 auth units (`src/lib/auth/session.test.ts`). CI: `npm run test:unit` replaces `supabase test db`. Docs: DEC-051 + DEC-048 amend + CLAUDE-context test refs.
- **76 vitest tests green** (unit + pg-integration, shared-DB serial); tsc + lint clean.

**Code review:** Faithful ports confirmed (4 parallel deep-dive comparisons vs deleted pgTAP — no SQLSTATE/message downgrades, ordering + fixtures preserved). 3 trivial type-annotation nits fixed (count/n string→number, dead local).
**PR:** [#273](https://github.com/mobiustripper42/bushel/pull/273)
**Points:** 5
**Branch:** task/10.4b-vitest-stack
**Opened at:** 2026-07-05T16:05:00Z

## Task 3: 10.4 — Consolidate to a single clean 0001 baseline (fold in login_codes/admins, retire 0002)

**Completed:**
- Eric's call: 0001 was billed "clean baseline" but accreted 0002 (auth tables). No prod data exists yet (docker + Neon `main` disposable, Neon prod empty until 10.7) → last window to consolidate. Rescoped #257 from "comment tweak" → baseline consolidation.
- `db/migrations/0001_init.sql`: folded in `admins` + `login_codes` tables + admins seed (Emma/Eric/Annabel) + table comments; finalized the `sent_by_user_id` comment (per-admin attribution = `admins.id`, bare uuid no FK); updated header. Deleted `0002_login_codes.sql`. A fresh DB now replays exactly one migration.
- Re-baselined docker dev+test locally (`drop schema public cascade` + re-migrate — the `_migrations` ledger already listed the old names, so a wipe was required for the edited 0001 to apply).
- Verified: single 0001 applies clean; `_migrations` count = 1; 3 admins + both auth tables present; 76 vitest green; 8 admin-auth login-flow specs green (exercises the folded tables end-to-end); build clean.

**Code review:** Clean Bill of Health — folded DDL + seed verified **byte-parity** with the deleted 0002; ordering correct; no collateral edits to place_order/other tables/indexes/triggers; no stray `0002` references.
**PR:** [#274](https://github.com/mobiustripper42/bushel/pull/274)
**Points:** 1
**Blocked:** post-merge, Eric must re-baseline Neon `main` (`drop schema public cascade; create schema public;` then `npx tsx db/migrate.ts "<neon-main-url>"` + re-seed preview fixtures) — in the PR body.
**Branch:** task/10.4-clean-baseline
**Opened at:** 2026-07-05T18:25:00Z

**Next Steps:**
- **After #274 merges: re-baseline Neon `main`** — `drop schema public cascade; create schema public;` then `npx tsx db/migrate.ts "<neon-main-url>"`, re-seed preview fixtures. Neon prod untouched (replays single 0001 at 10.7).
- **Env now lives in `.env.local`** (memory `feedback_env_vars_in_env_local`): DATABASE_URL, RESEND_API_KEY, RESEND_FROM (`@brewcle.com` — subdomain not verified, 403s), SESSION_SECRET. `.envrc` dies in 10.5.
- 10.5 (#258): remove `@supabase/*` deps + now-fully-dead supabase client files + strip supabase CLI/start/env from CI (pgTAP step already gone) + kill the direnv `.envrc` (DEC-049 env→.env.local) + retire the obsolete `feedback_supabase_cli_auth` memory.
- 10.6 (#259): headless login-code test helper — now largely done by vitest (login-code logic is unit-testable directly); the remaining bit is exercising the real request→verify headlessly. 10.7 (#260): Neon prod cutover.

**Context:**
- **login_codes stores only sha256(code)** — no test can read the emitted plaintext. global-setup mints the session directly; `admin-auth.spec` drives step 1 (real request) then overwrites the stored hash with a known code before verifying. 10.6's "read code from test DB" must use the same trick or a test-mode plaintext capture.
- **SESSION_SECRET must match** between the app process and global-setup (both read the env var; CI sets a fixed value; dev/test share the insecure default when unset).
