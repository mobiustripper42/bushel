---
session: 45
dev: eric
slug: 10.2-pg-data-layer
branch: task/10.2-pg-data-layer
started: 2026-07-05T12:12:13Z
ended: 2026-07-06T14:37:38Z
points: 21
pr_numbers: [271, 273, 274, 275, 276, 277]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/1a486546-1b51-4455-8d9e-f878299d45f2.jsonl
---

# Session 45 — 10.2-pg-data-layer

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 10.3 — Email-code admin auth (muster session.ts + login_codes + Resend, DEC-047)

**Completed:**
- Dropped Google OAuth / Supabase Auth for a self-rolled email→code→HMAC-session flow (DEC-047, supersedes DEC-003). closes #256.
- `db/migrations/0002_login_codes.sql`: `admins` allowlist (Emma/Eric/Annabel) + `login_codes` (muster 0012 port; `timestamptz` not `text` to match the pg baseline; no FKs per DEC-048). Test admin upserted by global-setup with a fixed id, NOT seeded — keeps `test-admin@bushel.test` out of prod.
- New `src/lib/auth/`: `session.ts` (pure node:crypto HMAC), `config.ts` (cookie/secret/TTLs, no next/headers so proxy imports it), `session-cookie.ts` (glue + sliding renewal; `secure` gated on `x-forwarded-proto` for the WebKit-over-http CI trap), `login-code.ts` (request/verify on direct pg — no-enumeration, 5-attempt cap, 10-min TTL, 60s cooldown, CAS consume), `email.ts` (Resend via fetch, no SDK).
- `getAdminUser` → HMAC session (14 call sites unchanged); `proxy.ts` verifies the cookie for `/admin` (Next 16 Node-runtime proxy); two-step login page; session-based sign-out + admin layout.
- Removed OAuth callback route + dead supabase middleware helper (other `@supabase` files now fully dead — 10.5 removes).
- Tests: global-setup mints the session directly (no supabase/inbox); `admin-auth.spec` drives the real request→verify flow (8 specs). CI gets a fixed `SESSION_SECRET`; `.env.example` documents the new keys.
- Verified locally against docker pg: build, tsc, lint, 8 auth specs + admin-action specs (getAdminUser attribution), WebKit 375px (admin session cookie over http).

**Code review:** Clean on all security-critical pieces (HMAC verify, CAS consume, attempt cap, no-enumeration, timing-oracle closed via `after()`). Fixed: `.env.example` now documents `SESSION_SECRET`/`RESEND_*`. Noted on #257 (10.4): `sent_by_user_id` id-space already flipped to `admins.id` for new writes (inert today — write-only, no FK).
**PR:** [#271](https://github.com/mobiustripper42/bushel/pull/271)
**Points:** 8
**Branch:** task/10.3-email-code-auth
**Opened at:** 2026-07-05T13:30:00Z

## Task 2: 10.4b — Adopt vitest test stack; migrate DB-invariant + auth-logic tests off pgTAP (DEC-051)

**Completed:**
- Rescope decision (Eric): pgTAP-on-docker was a hack; greenfield = keep the concurrency-critical logic in plpgsql, test it through the app's pg driver with **vitest**. Split #257 (10.4) → schema-only, **1 pt**; new **#272 (10.4b)**, 5 pts, is this task. New **DEC-051** + amends DEC-048.
- `vitest.config.ts` (`@/` alias, `fileParallelism: false` — integration files share one DB + truncate), `package.json` test:unit/test:watch.
- `db/tests/helpers.ts` — canConnect (skip cleanly if no DB), migrateTestDb, resetData, closePool. Integration tests drive plpgsql through `src/lib/db` against docker pg — no extension, no pg_prove.
- Ported all 6 function/data pgTAP → `db/tests/*.test.ts` via 4 parallel subagents on isolated DBs (bushel_test_a–d): place-order-additive (32 asserts), place-order-units (19), set-base-unit (17), product-units, order-status-codes, customer-sends-modes. RLS-only assertions dropped (DEC-048); the 3 RLS-only pgTAP files + all `supabase/tests/` deleted.
- Backfilled 10.3 auth units (`src/lib/auth/session.test.ts`). CI: `npm run test:unit` replaces `supabase test db`. Docs: DEC-051 + DEC-048 amend + CLAUDE-context test refs.
- **76 vitest tests green** (unit + pg-integration, shared-DB serial); tsc + lint clean.

**Code review:** Faithful ports confirmed (4 parallel deep-dive comparisons vs deleted pgTAP — no SQLSTATE/message downgrades, ordering + fixtures preserved). 3 trivial type-annotation nits fixed (count/n string→number, dead local).
**PR:** [#273](https://github.com/mobiustripper42/bushel/pull/273)
**Points:** 5
**Branch:** task/10.4b-vitest-stack
**Opened at:** 2026-07-05T16:05:00Z

## Task 3: 10.4 — Consolidate to a single clean 0001 baseline (fold in login_codes/admins, retire 0002)

**Completed:**
- Eric's call: 0001 was billed "clean baseline" but accreted 0002 (auth tables). No prod data exists yet (docker + Neon `main` disposable, Neon prod empty until 10.7) → last window to consolidate. Rescoped #257 from "comment tweak" → baseline consolidation.
- `db/migrations/0001_init.sql`: folded in `admins` + `login_codes` tables + admins seed (Emma/Eric/Annabel) + table comments; finalized the `sent_by_user_id` comment (per-admin attribution = `admins.id`, bare uuid no FK); updated header. Deleted `0002_login_codes.sql`. A fresh DB now replays exactly one migration.
- Re-baselined docker dev+test locally (`drop schema public cascade` + re-migrate — the `_migrations` ledger already listed the old names, so a wipe was required for the edited 0001 to apply).
- Verified: single 0001 applies clean; `_migrations` count = 1; 3 admins + both auth tables present; 76 vitest green; 8 admin-auth login-flow specs green (exercises the folded tables end-to-end); build clean.

**Code review:** Clean Bill of Health — folded DDL + seed verified **byte-parity** with the deleted 0002; ordering correct; no collateral edits to place_order/other tables/indexes/triggers; no stray `0002` references.
**PR:** [#274](https://github.com/mobiustripper42/bushel/pull/274)
**Points:** 1
**Blocked:** post-merge, Eric must re-baseline Neon `main` (`drop schema public cascade; create schema public;` then `npx tsx db/migrate.ts "<neon-main-url>"` + re-seed preview fixtures) — in the PR body.
**Branch:** task/10.4-clean-baseline
**Opened at:** 2026-07-05T18:25:00Z

**Next Steps:**
- **After #274 merges: re-baseline Neon `main`** — `drop schema public cascade; create schema public;` then `npx tsx db/migrate.ts "<neon-main-url>"`, re-seed preview fixtures. Neon prod untouched (replays single 0001 at 10.7).
- **Env now lives in `.env.local`** (memory `feedback_env_vars_in_env_local`): DATABASE_URL, RESEND_API_KEY, RESEND_FROM (`@brewcle.com` — subdomain not verified, 403s), SESSION_SECRET. `.envrc` dies in 10.5.
- 10.6 (#259): headless login-code test helper — now largely done by vitest (login-code logic is unit-testable directly); the remaining bit is exercising the real request→verify headlessly. 10.7 (#260): Neon prod cutover.

## Task 4: 10.5 — Remove the Supabase footprint (deps, dead files, CI, docs) — DEC-049

**Completed:**
- Supabase unused by the app since 10.3 (auth→HMAC) — removed the carcass. closes #258.
- Deleted `src/lib/supabase/` (4 dead files) + the whole `supabase/` dir (config + 27 legacy migrations + seed); removed `@supabase/ssr` + `@supabase/supabase-js` from package.json + lock (Eric ran `npm install` — 12 packages pruned, 0 `@supabase` refs remain).
- CI: dropped `supabase/setup-cli` + `Start Supabase` + `Set Supabase env vars` steps + the 3 `-e …SUPABASE…` container passthroughs; refreshed stale comments.
- `.claude/CLAUDE-context.md`: rewrote Stack, Commands, Migration Protocol (Neon branch model + explicit-arg prod via `.envrc.production`, DEC-049), Env (`.env.local`, no direnv, DATABASE_URL_UNPOOLED for migrations), Data Fetching / Auth (pg + HMAC session, service-layer boundary), Naming, preview block — removed the two-project link/relink dance, OAuth allowlist, RLS, pgTAP refs. Dropped orphaned `source .envrc` permission from `settings.local.json`. Retired `feedback_supabase_cli_auth` memory.
- `.envrc` is gitignored → direnv teardown (`rm .envrc && direnv deny`) is a local shell step, not in the PR.
- Verified: build (Proxy intact), 76 vitest, tsc, lint green against the pruned tree.

**Code review:** Clean & complete — 0 `@supabase` imports remain (only historical why-comments); CI flow intact, no dangling supabase env vars; lock consistent. 2 nits fixed: CLAUDE-context self-contradiction (`.envrc.production` vs "no .envrc") + `PROD_DATABASE_URL` name mismatch with DECISIONS.md.
**PR:** [#275](https://github.com/mobiustripper42/bushel/pull/275)
**Points:** 3
**Branch:** task/10.5-remove-supabase (branched pre-#274; disjoint files, GitHub auto-merges)
**Opened at:** 2026-07-05T20:10:00Z

**Gotcha logged:** ended up on `main` with 10.5 changes uncommitted (branch didn't stick at some point) — caught at `/kill-this` Step 0, switched to the task branch (uncommitted changes carried over cleanly since it branched from main with no commits). Bushel is PR-flow; never commit 10.x to main directly.

## Task 5: 10.6 — vitest pg-integration coverage for login-code request/verify (DEC-051)

**Completed:**
- Reframed #259: the headless-admin-auth win it targeted already landed in 10.3 (global-setup mints the HMAC session directly). Kept the storageState fixture (fast/deterministic — a per-test code-login helper would be a speed regression for zero coverage). Real gap = `login-code.ts` logic, only lightly hit by admin-auth.spec E2E.
- `db/tests/login-code.test.ts` (new, 12 tests): drives `requestLoginCode` + `verifyLoginCode` against docker pg + real admins/login_codes, injected clock + code generator (ported from muster). Covers request (match / case-insensitive+trim / unknown-skip / deactivated-skip / cooldown-suppress-then-remint) and verify (single-use CAS replay / wrong-counts-cap / lock-at-5th-attempt / expired / non-matching-email) + pure helpers.
- Full vitest suite 88 green.

**Code review:** No false-green risk (cooldown math, lock loop, CAS replay, isolation, determinism all independently verified against the real DB). 3 coverage-fidelity nits folded in: assert cooldown-survivor is still redeemable, assert `attempts=0` on first mint, add `subject_kind='admin'` to the attempts query.
**PR:** [#276](https://github.com/mobiustripper42/bushel/pull/276)
**Points:** 2
**Branch:** task/10.6-login-code-tests (off main; `@supabase` dead-file tsc errors were a local node_modules-prune artifact — reconciled via `npm install`, clean in CI)
**Opened at:** 2026-07-05T20:50:00Z

**Next Steps (Phase 10 remaining):**
- **10.7 (#260): Neon prod cutover** — the coordinated step: migrate Neon `production` (single 0001, via `.envrc.production` unpooled URL), move/seed prod data, then `/promote-production` (main→production). Do NOT `/promote-production` before this — main carries pg + supabase-removed code while prod data is still on Supabase.
- **Merge order:** #274 (baseline) already merged + Neon `main` re-baselined by Eric. #275 (10.5 supabase removal) + #276 (10.6 tests) open — disjoint, either order; GitHub auto-merges. After #275 merges, `npm install` will prune `@supabase` locally again.

## Task 6: 10.7 — Neon prod cutover runbook + guarded catalog-migration script (#260)

**Completed:**
- Eric asked to prep 10.7 so he can review + execute in one sitting. 10.7 is a deliberate deploy step, so the repo artifact is a runbook + script; the cutover execution is Eric's (cloud/prod/deploy).
- `docs/RUNBOOK-neon-cutover.md`: ordered cutover — pre-flight (Vercel Production env: Neon prod `DATABASE_URL` + `SESSION_SECRET` + `RESEND_*`; grab Supabase-prod DIRECT URL) → schema to Neon prod (`db/migrate.ts` via `.envrc.production` unpooled) → catalog data move → `/promote-production` (DB before code) → verify → clean rollback (Supabase read-only throughout).
- `scripts/migrate-prod-catalog.sh`: data-only `pg_dump` of products/product_units/customers (FK-safe order) Supabase→Neon prod. Guards: schema-present, all-3-tables-empty idempotency, `yes` prompt, per-table row-count verify, credential redaction. Orders/order_items/customer_sends NOT moved (wiped per DEC-041).
- Scope per #260: only the 3 catalog tables carry over; ordering_schedule.is_open + fulfillment_link.token NOT migrated (re-open ordering + reshare `/f/` link post-cutover — flagged in runbook).

**Code review:** Guardrails sound — no data-loss path (read-only Supabase, FK order correct, `pipefail`+`ON_ERROR_STOP` abort partial loads, empty-target guard). 4 fixes folded in: corrected stale DEC-047 `RESEND_FROM` (→ `@brewcle.com`, subdomain 403s — real; would've misled prod-email setup); dropped inert `DATABASE_URL_UNPOOLED` from Vercel list; guard all 3 tables; de-hardcoded rollback version.
**PR:** [#277](https://github.com/mobiustripper42/bushel/pull/277)
**Points:** 2
**Blocked:** the cutover EXECUTION is Eric's — do NOT `/promote-production` until the runbook's DB steps (migrate Neon prod + catalog move) are done. #260 closes when prod is cut over + verified, not when this PR merges.
**Branch:** task/10.7-prod-cutover-runbook
**Opened at:** 2026-07-05T21:15:00Z

**Context:**
- **login_codes stores only sha256(code)** — no test can read the emitted plaintext. global-setup mints the session directly; `admin-auth.spec` drives step 1 (real request) then overwrites the stored hash with a known code before verifying. 10.6's "read code from test DB" must use the same trick or a test-mode plaintext capture.
- **SESSION_SECRET must match** between the app process and global-setup (both read the env var; CI sets a fixed value; dev/test share the insecure default when unset).

**Session 45 close (Phase 10 complete + live in prod):**
- Phase 10 shipped, retro'd, closed at **v0.12.0**; prod cut over Supabase→Neon + email-code auth, verified working, promoted (production at v0.12.0). Catalog (products/product_units/customers) migrated to Neon prod; orders wiped per DEC-041.
- **Manual bump note:** shipped a stopgap v0.11.2 mid-session (cutover needed to ship before phase close); /retro reconciled to v0.12.0 with the full per-PR CHANGELOG. Correct order is retro→promote; this inverted once for the cutover.
- **Phase 11 restructured (DEC-052 via @architect):** bushel-mobile is an independent seeds project, NOT a satellite; a phase never spans repos. 11.1 (#261) stays a bushel phase (5pt, /api routes — do first, it's the contract the app calls); #262–#265 → bushel-mobile Phase 1 (15pt). Bootstrap handoff written to `docs/BUSHEL-MOBILE-KICKOFF.md` for a fresh multi-repo CC session to execute.
- **Open items:** #278 (UX mobile pass / full UI review), #279 (document preview-testing + prod→main catalog copy). prod→main catalog copy is a one-liner in #279, run whenever for realistic preview data. Neon + Vercel MCPs installed (user scope) — restart + `/mcp` auth to use them.
- **Supabase prod project:** keep alive a few days as a rollback net, then decommission (`scripts/safe-supabase.sh` is orphaned now too).
