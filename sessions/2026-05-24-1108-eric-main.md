---
session: 31
dev: eric
slug: main
branch: main
started: 2026-05-24T11:08:40Z
ended: 2026-05-24T15:44:49Z
points: 7
pr_numbers: [162, 163, 164, 165]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/7bf296d6-7973-4be6-aca8-2e60315db934.jsonl
---

# Session 31 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 6.5c — Customer order form: per-product unit picker (#153)

**Completed:**
- `src/lib/customer/queries.ts` — joins `product_units` into `getAvailableProducts()`. New `ProductUnitRow` type alias; active-only filter + `(sort_order nulls last, created_at)` sort happens JS-side after the join (service-role bypass of RLS is intentional per DEC-018; the JS filter is the inactive-unit gate).
- `src/components/customer/OrderForm.tsx` — new `selectedUnit` state, `unitFor()` resolver (handles stale selections gracefully via fallback to first active unit). Item-row markup restructured: name owns its row, `.item-meta-row` carries price + meta, `.unit-picker` radio group below. `perUnitMax = Math.floor(qty_available / conversion_to_base)`. Submit payload snapshots selected unit's `unit_price_cents`. `Math.max(0, …)` on `remaining` so a mid-session inventory drop can't render "only -2 left".
- `src/styles/app.css` — restructured `.item-row` with `align-items: start`; new `.unit-picker` / `.unit-option` primitives. `.item-name` wraps freely (closes #144). `:has(input:focus-visible)` keyboard ring on the picker labels (native radio is `opacity:0`).
- `design/order-page.{jsx,css}` — lockstep mockup update per the design-folder-is-authoritative rule.
- `tests/customer-order-units.spec.ts` — 6 cases on desktop + mobile webkit: picker rendering, qty reset on switch, per-unit sold-out radio gate, single-unit regression, submit payload `unit_price_cents` snapshot, long-name wrap at 375px.
- `tests/helpers.ts` — new `setProductUnits()` for multi-unit test fixtures.

**Code review:** Two real correctness items + one a11y item flagged and addressed in commit `28f3f89` — `Math.max(0, remaining)` clamp, `:has(input:focus-visible)` outline, and the unit_price_cents payload assertion. `conv=0` divide concern is enforced at the DB layer (`check (conversion_to_base > 0)`). useCallback / anon-client nits deferred.
**Known gap (intentional, sequenced for 6.5d):** `place_order` RPC still treats qty as base units and `order_items.product_unit_id` falls through the 6.5a safety-net trigger to the base unit. Selected unit's *price* is recorded on the order row; unit-id + decrement math wait for 6.5d (#154).
**PR:** [#162](https://github.com/mobiustripper42/bushel/pull/162)
**Points:** 2 (per issue label)
**Branch:** task/6.5c-customer-unit-picker
**Opened at:** 2026-05-24T12:33:09Z

## Task 2: 6.5d — Unit-aware place_order: fractional decrement + price snapshot (#154)

**Completed:**
- `supabase/migrations/20260524141544_place_order_unit_aware.sql` — replaces the `place_order` function body. Signature unchanged. p_items elements grew optional `product_unit_id`; RPC resolves the unit (payload id → product-scoped fallback → "no active units" raise), looks up `unit_price_cents` + `conversion_to_base` from `product_units`, and decrements `qty_available` by `qty * conversion_to_base`. Client-supplied `unit_price_cents` is ignored.
- `src/actions/place-order.ts` — `PlaceOrderItem` gains `product_unit_id`.
- `src/components/customer/OrderForm.tsx` — payload threads selected unit's id; per-product `out` detection now considers whether any active unit fits in remaining base inventory.
- `src/app/c/[token]/page.tsx` — `anyOrderable` check uses the new per-unit conversion test (a product with qty=2 and only a conv=4 unit is now correctly "sold out").
- `supabase/tests/place_order_units.sql` — 13 new pgTAP cases (107 total): fractional decrement, payload-vs-DB price reconciliation, snapshot survives unit-price edits, oversell-by-unit reconciliation, single-unit regression, trigger fallback, distinct "no active units" error path.
- `tests/customer-order-units.spec.ts` — payload spec upgraded to verify `product_unit_id` matches the selected lb unit AND `qty_available` decremented by `qty * conversion_to_base`. The 6.5c "known gap" comment retired.
- `docs/OPEN_ITEMS.md` — triage entry for `product_units.is_whole_only` (lettuce-by-the-bag vs. basil-by-the-lb fractional ordering, surfaced by user mid-session).

**Code review:** One real issue addressed in commit `1a42d12` — misleading error when payload omits `product_unit_id` for a product with no active units. Now raises "product X has no active units" instead. Numeric precision, migration safety (`create or replace` same signature), and prod-push sequencing all confirmed clean (old RPC ignores extra JSON keys → 6.5c client + old RPC is forward-compatible during the merge gap).
**PR:** [#163](https://github.com/mobiustripper42/bushel/pull/163) (stacked on #162; base auto-retargets to main when #162 merges)
**Points:** 2 (per issue label)
**Branch:** task/6.5d-fractional-decrement (stacked off task/6.5c-customer-unit-picker)
**Opened at:** 2026-05-24T14:33:07Z

## Task 3: 6.5e — Unit-aware pre-populate + last-week price restore (#155)

**Completed:**
- `supabase/migrations/20260524144640_prepopulate_unit_aware.sql` — rewrites `prepopulate_inventory_from_last_week()` in plpgsql. Qty restore sums `qty * conversion_to_base` per product (inverse of 6.5d's decrement); each unit's `unit_price_cents` reverts to its most recent last-week `order_items` snapshot. Untouched units left alone; deactivated units NOT re-enabled (explicit scope decision). Return type widens to `(uuid, numeric)` — required a `drop function` since PostgreSQL rejects return-signature changes inside `create or replace`.
- `src/components/admin/prepopulate-button.tsx` — confirm-dialog copy updated to mention the price-reset side-effect so Annabel sees it before clicking through.
- `supabase/tests/prepopulate_units.sql` — 9 pgTAP cases (116 total): multi-unit fractional reversal, single-unit regression, price restore to snapshot, untouched-unit price preserved, no-prior-week no-op, deactivated-unit invariant pin.
- `tests/admin-prepopulate.spec.ts` — new "multi-unit pre-populate (6.5e)" describe seeds Kale as bunch + lb with snapshot prices distinct from current, asserts qty restored unit-aware AND both unit prices reverted to snapshot. Outer afterEach now defensively cleans both fixture ids + resets units unconditionally (closes a poison-between-tests risk on partial inner-afterEach failure).

**Code review:** Three findings — defensive outer afterEach, stale qty-reset block in pgTAP, missing scope-pin for deactivated-unit invariant. All addressed in commit `26be5ba`. SQL math + plpgsql conversion + price-update `is distinct from` guard confirmed correct.
**PR:** [#164](https://github.com/mobiustripper42/bushel/pull/164) (stacked on #163; chain is #162 → #163 → #164 → main)
**Points:** 1 (per issue label)
**Branch:** task/6.5e-prefill-units (stacked off task/6.5d-fractional-decrement)
**Opened at:** 2026-05-24T14:59:10Z

## Task 4: 6.5f — Per-line unit label everywhere + cross-cutting e2e tests (#156)

**Completed:**
- Discovered + fixed a real bug: `listOrders()` and `getCurrentWeekOrder()` were pulling `products.unit` (legacy base label) for line-item display, so the admin orders expand row and customer /confirmed page were showing the WRONG unit for multi-unit orders. Rolled the fix into 6.5f alongside the named "cross-cutting tests" scope.
- `src/lib/admin/orders-queries.ts` — `listOrders()` joins `product_units(label, conversion_to_base)`. `OrderItem` gains `unitLabel` (per-line) and `conversionToBase` (for unit-aware oversold math).
- `src/components/admin/order-detail.tsx` — line items render `i.unitLabel`. Oversold check upgraded to `qty * conversionToBase > qtyAvailable`. Display reframed in selected-unit. `qtyAvailable` clamped to ≥ 0 for display under DEC-012 (a negative post-decrement value rendered as "-1.5 lb available" pre-fix).
- `src/lib/customer/queries.ts` + `src/app/c/[token]/confirmed/page.tsx` — same per-line unit label fix on the customer confirmation page.
- `tests/orders-flow.spec.ts` — new "multi-unit end-to-end (6.5f)" describe: happy path (customer picks lb → admin sees correct label + $10.00 line + qty decremented by 2×conv) and oversell-by-unit (mid-session qty drop triggers `needs_reconciliation` + clamped "0 lb available" copy).

**Code review:** Three findings — mixed unit frames in oversold copy, naive pluralization broke "bunch", and the oversell test caught a `negative qty_available → "Only -1.5 lb available"` display bug. All addressed in commit `0181fce`. Display fallback to `products.unit` is deliberate belt-and-suspenders.
**PR:** [#165](https://github.com/mobiustripper42/bushel/pull/165) (stacked; chain is #162 → #163 → #164 → #165 → main)
**Points:** 2 (per issue label)
**Branch:** task/6.5f-cross-cutting-tests (stacked off task/6.5e-prefill-units)
**Opened at:** 2026-05-24T15:42:49Z

**Next Steps:**
- Merge the Phase 6.5 stack in order: #162 → #163 → #164 → #165. GitHub auto-retargets each next PR's base to `main` as its parent merges. Then push the four migrations to prod: `source .envrc && supabase link --project-ref piaobrnrmoxnfrpnpixw && supabase db push && supabase link --project-ref nnmfubmlvnkouxxfxxlh`.
- Once the stack lands, run `/retro` to close Phase 6.5 (reads PR merge timestamps, computes velocity, writes RETROSPECTIVES.md, bumps version).

**Context:**
- Stacked PRs: #162 → #163 → #164 → #165 must merge in order. GitHub auto-retargets each next PR's base to `main` only when its current parent merges. If you merge out of order (say #165 first), GitHub will refuse or create a tangled diff — don't do that.
