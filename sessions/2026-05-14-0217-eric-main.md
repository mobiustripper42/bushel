---
session: 19
dev: eric
slug: main
branch: main
started: 2026-05-14T02:17:49Z
ended: 2026-05-14T03:43:01Z
wall_clock: 1.42
dev_time: 1.33
review_time: 0.08
duration: 1.33
points: 3
status: closed
pr_number: 70
pr_url: https://github.com/mobiustripper42/bushel/pull/70
pr_opened_at: 2026-05-14T03:36:29Z
transcript: /home/eric/.claude/projects/-home-eric-bushel/996cafef-3f32-4240-bd1d-93cc54367f30.jsonl
---

# Session 19 — main

**Task:** Issue #60 — Infra: stand up `preview.baybranchfarm.com` as a stable mobile-review URL, verify prod env vars, get Google OAuth working end-to-end on a deployed preview.

**Completed:**
- **PR #70 merged** (closes #60, 3pts). 4 files: CLAUDE.md, src/app/auth/callback/route.ts, .claude/settings.local.json (allowlist), session file.
- **Prod 500 fixed** — Vercel Production was missing/misnamed env vars after the bushel↔sailbook account split. URL + anon key + service-role key all verified against `.env.local`; redeploy → `order.baybranchfarm.com` HTTP 200.
- **Preview env vars** mirrored (set to both Production + Preview envs in one pass).
- **`preview.baybranchfarm.com` live** — Cloudflare CNAME auto-configured by Vercel domain add; per-project Vercel CNAME (DNS-only, gray cloud); Let's Encrypt cert provisioned in ~5 min; HTTP 200.
- **`/auth/callback` rewritten** — old code used the `cookies()`-from-`next/headers` helper which writes into an implicit response that `NextResponse.redirect()` discards. Sessions never persisted. Now mirrors `src/lib/supabase/middleware.ts` — cookies attach directly to the redirect response. Latent bug that meant OAuth had never worked on any deployed env.
- **Supabase URL Configuration** — Site URL set to prod canonical; redirect allowlist includes `https://preview.baybranchfarm.com/**` (and prod, vercel.app, mill-dev:3001). Double-star, not single-star — single-star burned an hour of "auth almost works" debugging because Supabase silently falls back to Site URL on a non-match, landing the user at `/?code=...` with the callback route never running.
- **CLAUDE.md additions** — §Migration Protocol "Cross-system env-var sync (Supabase ↔ Vercel)" with diff-check command + variable-name-typo gotcha. §PR Workflow "Stable preview URL — preview.baybranchfarm.com" with one-time setup, per-branch reassignment, Supabase OAuth allowlist (with `/**` vs `/*` callout), and the 404/500 fresh-branch-no-SHA gotcha.
- **Issue #69 filed** — "Auto-rebind preview.baybranchfarm.com at branch cut" (option B from the manual-vs-CLI-vs-GHA conversation). Vercel CLI one-liner wrapped in `scripts/preview-rebind.sh`, called from `/its-alive` Step 0. Not in this PR — out of scope.
- **Memory: `feedback_no_infra_smoke_tests.md`** — user explicitly rejected the "phone-test Playwright job" stretch goal in #60. Don't propose CI smoke tests for deployment/OAuth/DNS infra on bushel going forward.

**In Progress:** Nothing.

**Blocked:** Nothing.

**Next Steps:**
1. **Retest OAuth on prod** (`order.baybranchfarm.com/login` → Google → `/admin/inventory`). Just a confirm — same code path, same allowlist as preview.
2. **Issue #69 (2pts)** — Vercel CLI auto-rebind on branch cut. Opportunistic; pick up when next cutting a task branch and feel the manual-click cost again.
3. **Resume Phase 3.3 (#48, 3pts)** — `/c/[token]` route + cookie + token-rotation invalidation. Original session-17 next step that sessions 18 + 19 both diverted from.
4. **#66 admin shell, #67 pgTAP DEC-029/030** — Phase 3 polish, opportunistic.
5. **SPEC.md + USER_STORIES.md** — first-pass drafts still flagged "pending review."

**Context:**
- **OAuth callback bug had been latent since the route was written.** Never caught because deployed envs were 500'ing for unrelated env-var reasons until tonight. The fact that one infra ticket surfaced and fixed a real product bug is a good argument for actually exercising deployed previews regularly, not just localhost. Adding this to mental rotation.
- **`/**` vs `/*` cost an hour.** Documented in CLAUDE.md PR Workflow with explicit "session 19 burned this exact hour" callout. The failure mode is particularly nasty because Supabase silently falls back to Site URL — no error, no warning, OAuth "works" from Supabase's perspective. Worth checking whether `/seeds` upstream has this documented for the next project that wires Supabase + custom preview URL.
- **`__cf_bm` cookie warning is noise** — Cloudflare bot-management cookie served by `supabase.co` with `Domain=supabase.co`. Browsers block as third-party. Unrelated to our session cookies (which we set on our own host). Worth knowing because it's a tempting red herring when debugging auth.
- **No Playwright test for OAuth.** User direction: don't propose automated smoke for deployment-adjacent infra. Saved as `feedback_no_infra_smoke_tests.md`. Real-usage signal is the test here.

**Code Review:** Clean bill from @code-review. Three nits raised, two folded in (`request.nextUrl.origin` + 404/500 wording precision), one (add Playwright spec for callback) explicitly rejected per user direction.
