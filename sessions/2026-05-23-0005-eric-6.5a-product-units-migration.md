---
session: 30
dev: eric
slug: 6.5a-product-units-migration
branch: task/6.5a-product-units-migration
started: 2026-05-23T00:05:46Z
ended:
points:
pr_numbers: [160]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/a9a79a20-02b7-4fd8-ab80-af137286e9ec.jsonl
---

# Session 30 — 6.5a-product-units-migration

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 6.5b — Per-product Units sub-sheet (#152)

**Completed:**
- Mockup first in `design/admin-inventory.jsx` + `design/admin-inventory.css`; built `design/preview-inventory.html` as a Babel-standalone harness so the canonical JSX is live-renderable. Screenshotted single-unit + multi-unit drawer states for design sign-off.
- `src/components/admin/units-drawer.tsx` — new drawer + UnitRow components. Mirrors `customer-drawer.tsx` shell (scrim + drawer-head/body/foot + ESC). Base unit identified by `conversion_to_base = 1.0` (no `is_base` flag in schema); BASE badge, locked conv, no delete.
- `src/components/admin/inventory-row.tsx` — `.units-chip` affordance under the unit cell. Dashed `+ Add unit` for single-unit products, solid green pill with count badge for multi.
- `src/components/admin/inventory-editor.tsx` — drawer open/close state + `router.refresh()` after save.
- `src/app/(admin)/admin/inventory/page.tsx` — joined `product_units` into the page query, keyed by product_id.
- `src/actions/save-product-units.ts` — new Server Action. Validates ≥1 active, conversion > 0, label non-blank, labels unique within product, non-negative price. Slug computed in the 6.5a trigger's format. FK-violation on delete (`23503`) returns Annabel-facing copy.
- `src/styles/app.css` — `.units-*` + `.units-chip` primitives (one-file CSS rule, no per-page file).
- `tests/admin-inventory-units.spec.ts` — 6 cases passing on desktop **and** mobile webkit: chip states, add, edit (inactive subtext), remove non-base, validation chain (blank/dup/zero-conv/all-inactive), new-row chip suppression.
- `tests/helpers.ts` — new `resetProductUnits` (delete-all + reinsert base with canonical slug). The naive "delete extras + restore base" approach was insufficient — failed test runs left base rows with mismatched slugs that broke the next run's slug-uniqueness check.

**Code review:** Two real fixes addressed in `4ea7ced` — FK-violation message on delete + `deletedIds` invariant comment. Latent items flagged in PR body: slug-format divergence between trigger (`name-id8`) and new units (`name-label-id8`), and client/server validation-order mismatch.
**PR:** [#160](https://github.com/mobiustripper42/bushel/pull/160)
**Points:** 5 (per issue label)
**Branch:** task/6.5b-inventory-units-subsheet
**Opened at:** 2026-05-23T13:19:29Z

**Next Steps:**

**Context:**
