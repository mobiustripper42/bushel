---
session: 1
dev: eric
slug: kickoff
branch: task/0.2-nextjs-scaffold
started: 2026-05-06T02:07:19Z
ended: 2026-05-06T03:21:19Z
duration: 1.25
points: 3
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/b171241f-0dc2-450a-a755-43519818e8b3.jsonl
---

# Session 1 — kickoff

**Task:** Phase 0.2 — Next.js 16 + Tailwind v4 + shadcn baseline scaffold (#3)

**Completed:**
- Materialized Phase 0 as GitHub Issues #3–#7 with `phase:0` + `points:N` labels (and wrote them back into PROJECT_PLAN.md)
- Test-drove a GitHub project board for #3–#7, decided no — too much for solo phase, kept the recipe in memory for "when a 2nd dev joins"
- Cut `task/0.2-nextjs-scaffold`; ran `create-next-app@latest` in `/tmp` and merged scaffold around `docs/`, `sessions/`, `.claude/` via mv-aside
- Added shadcn baseline deps (`clsx`, `tailwind-merge`, `class-variance-authority`, `tw-animate-css`, `shadcn`)
- Replaced `globals.css` with sailbook's `@theme inline` + OKLCH token block (placeholder until 0.7)
- `layout.tsx`: Nunito Sans + dev/preview env strip-bars; `page.tsx`: "Bay Branch Farm — coming soon" placeholder; `components.json` (radix-maia, neutral, lucide); `src/lib/utils.ts` (`cn` helper)
- `npm run build` green; localhost smoke-tested; deployed to bushel.vercel.app — HTTP 200 confirmed
- PR #8 opened with code-review findings in body — merged (closed #3, +3 pts)
- Mid-session: bumped #4 (Phase 0.3) from 2 → 3 pts to absorb two pre-work items — rename dev host (currently `sailbook-dev`), pin bushel to a fixed non-3000 dev port. Both must land before Google OAuth redirect URIs are configured.

**In Progress:**
- CNAME `order.baybranchfarm.com` → `cname.vercel-dns.com` (Cloudflare, gray-cloud) still to wire — code is on prod via bushel.vercel.app, but the branded domain isn't live yet

**Blocked:** none

**Next Steps:**
1. Add CNAME in Cloudflare (proxy off), wait for cert, verify https://order.baybranchfarm.com serves the same page
2. (Optional follow-up) address the two code-review notes from PR #8: (a) delete root `AGENTS.md` or fold into `README.md`, (b) drop `suppressHydrationWarning` until ThemeProvider arrives — or defer both to a later session
3. Next session: start #4 (Phase 0.3, now 3 pts) — pre-work first (host rename, port pin), then Supabase + Google OAuth + customer token column

**Context:**
- gh token now has `read:project` + `project` scopes (added mid-session); board was created and deleted; recipe is in memory for re-creating when a 2nd dev joins
- `create-next-app@latest` (16.2.4) ignored `--skip-git`; we worked around by moving its `.git` aside post-init. Worth knowing for the next CNA-based scaffold.
- Sailbook lives at `/home/eric/sailbook` and was the source of truth for `globals.css` and `components.json` shape
- Phase 0 point total drifted 19 → 20 — to be reconciled at `/retro` per the workflow (PROJECT_PLAN.md is read-only mid-phase)
- `/kill-this` and `/its-dead` slash commands didn't resolve through the harness this session — the move-aside dance for `.claude/` during scaffolding seems to have de-registered them. Both were executed inline by reading their `SKILL.md` files. Next session start should re-pick up the registrations.
- Memory updated: `feedback_optional_steps.md` (don't silently skip optional integrations), `project_solo_no_board.md` (Issues only while solo)

**Code Review:** 🟢 ship-it. Two minor notes (root `AGENTS.md` collision; `suppressHydrationWarning` without ThemeProvider) captured in PR #8 body — actionable but non-blocking.
