---
session: 22
dev: eric
slug: main
branch: main
started: 2026-05-14T23:23:00Z
ended: 2026-05-15T02:11:41Z
points: 10
pr_numbers: [80, 81]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/a7b1df23-af4c-4f6d-bc67-4390fb765794.jsonl
---

# Session 22 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Infra — Auto-rebind preview.baybranchfarm.com (#69)

**Completed:**
- `scripts/preview-rebind.sh`: idempotent shell script that PATCHes the Vercel project-domain `gitBranch` field via API (`/v9/projects/bushel/domains/preview.baybranchfarm.com`). Soft-fails on no token, on main/master, or on API error. Note: `vercel alias set` 403'd — the domain is a project-domain git-branch binding, not a deployment alias. PATCH is what the dashboard's Edit → Git Branch button hits.
- `.claude/skills/its-alive/SKILL.md`: new Step 0.4 invokes script on `task/*` / `claude/*` branches.
- `CLAUDE.md` "Stable preview URL" section rewritten: auto-rebind via script is default; manual dashboard click is fallback. `VERCEL_TOKEN` added to `.envrc` requirements.
- End-to-end verified: `preview.baybranchfarm.com` rebound to `task/infra-preview-rebind` (HTTP 200 serving the branch's build).
- Outstanding: user needs to create `VERCEL_TOKEN` PAT (Account Settings → Tokens, scope = mobiustripper) and add `export VERCEL_TOKEN=...` to `.envrc`.

**PR:** opened by user (skills broken this session after /model switch — "Unknown command: /kill-this")
**Points:** 2
**Branch:** task/infra-preview-rebind

## Task 2: Phase 3.6 — Manual open/close toggle + weekly schedule + cron (#51)

**Completed:**
- `src/app/(admin)/admin/settings/page.tsx`: server component, reads `ordering_schedule` singleton, renders explicit error on DB failure.
- `src/components/admin/SettingsScheduleCard.tsx`: client component; Open now / Close now / Resume schedule buttons; weekly day+time pickers disabled when schedule off; iOS-style toggle; sticky save bar with Discard + Save.
- `src/actions/save-schedule.ts`: saves `weekly_*` columns (or nulls them when schedule off).
- `src/actions/toggle-ordering.ts`: open / close / resume actions; `revalidatePath` after each.
- `src/app/api/cron/check-schedule/route.ts`: Vercel Cron (every 5 min), protected by `CRON_SECRET`, uses `createAdminClient()` to bypass RLS; handles `override_closes_at` expiry + weekly schedule transitions.
- `vercel.json`: cron entry `*/5 * * * *`.
- `src/styles/app.css`: ~250 lines of `.set-*` styles.
- `tests/admin-settings.spec.ts`: 5 tests — render, save bar dirty/clean, discard, open/close toggle, fields disabled when off. Fixed: replaced `waitForLoadState("networkidle")` with element-based hydration waits (Supabase WS blocked networkidle). 5/5 green.
- Key fix during review: `isOpen` lifted to local state + `router.refresh()` so UI updates after toggle without full re-navigate.

**Outstanding:**
- `CRON_SECRET`: add `export CRON_SECRET=<random>` to `.envrc` AND Vercel env vars (Production + Preview).
- UTC/NY timezone drift in cron is a V1 known limitation — should be a GitHub issue.

**PR:** https://github.com/mobiustripper42/bushel/pull/81
**Points:** 8
**Branch:** task/3.6-settings-schedule

**Next Steps:**

**Context:**
