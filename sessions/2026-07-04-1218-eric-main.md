---
session: 44
dev: eric
slug: main
branch: main
started: 2026-07-04T12:18:23Z
ended:
points:
pr_numbers: [267, 268]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/361b9d14-aed7-4c54-936b-a984b84425b7.jsonl
---

# Session 44 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 10.0 — Neon project + branches + docker CI Postgres

**Completed:**
- Pre-task: promoted main → production at v0.11.1 (91 commits, DEC-041 cutover live; Eric ran the prod `db push` beforehand). Hand-bumped v0.11.1 (patch for #266) since /retro wasn't due — CHANGELOG + tag on main.
- `docker-compose.yml` — postgres:17, bushel_dev + bushel_test, host port 5433 (muster owns 5432); `db/init/01-create-test-db.sql`
- `.github/workflows/ci.yml` — postgres:17 service + sanity step (Supabase steps stay until 10.5/10.6)
- `db/roles/mcp_readonly.sql` — applied to Neon `production` branch; login verified, writes denied; URL saved in `.envrc.production` as `MCP_READONLY_DATABASE_URL`
- Neon project via Vercel Storage integration; branches `main` (dev/preview) + `production`; Vercel env split per scope (Production → production branch, Preview → main branch; Development scope was locked in dashboard, skipped — only feeds `vercel dev`)
- `.envrc` (dev URLs) + `.envrc.production` (PROD_* URLs) filled by Eric; `.gitignore` covers `.envrc.production`

**Code review:** 1 finding fixed (documented ALTER DEFAULT PRIVILEGES role-binding assumption — must run as neondb_owner, same role as prod migrations); 2 advisory (neondb name documented, CI healthcheck cadence drift vs muster intentional)
**PR:** [#267](https://github.com/mobiustripper42/bushel/pull/267)
**Points:** 3
**Branch:** task/10.0-neon-setup
**Opened at:** 2026-07-04T12:18:23Z

## Task 2: 10.1 — Port db/migrate.ts runner + 0001_init.sql clean baseline

**Completed:**
- Pre-task: merged #267 (squash), 10.0 closed. Answered MCP-auth question: the `mcp_readonly` password lives inside `MCP_READONLY_DATABASE_URL`; actual MCP server wiring deferred (move var to `.envrc` when wanted).
- `db/migrate.ts` — muster port (byte-identical logic; bushel names, port 5433, DEC-049 explicit-arg prod form)
- `db/migrations/0001_init.sql` — clean baseline from schema dump of final Supabase state; DEC-048 deletions applied (no RLS, no public.users, no auth.users FKs; customer_sends.sent_by_user_id kept as bare uuid). Seed: 7 codes rows + both singletons. NOTE: no send_queue table exists — DEC-048's mention maps to customer_sends.sent_by_user_id.
- `package.json` — db:up/db:down/db:migrate; pg in dependencies (review fix), @types/pg + tsx devDeps
- Verified: idempotent apply on docker bushel_test; place_order smoke (create/append/replay + trigger + qty math); applied to Neon `main` (wiped + re-applied after the index-comment fix so the applied file matches the committed file)

**Code review:** 2 findings fixed (restored COMMENT ON INDEX orders_one_open_per_customer; pg → dependencies). Advisory for 10.7 runbook: pg sslmode warning on stderr during Neon connects is noise. Review confirmed baseline parity programmatically.
**PR:** [#268](https://github.com/mobiustripper42/bushel/pull/268)
**Points:** 5
**Branch:** task/10.1-migrate-runner
**Opened at:** 2026-07-05T12:30:00Z

**Next Steps:**

**Context:**
