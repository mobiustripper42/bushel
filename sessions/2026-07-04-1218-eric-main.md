---
session: 44
dev: eric
slug: main
branch: main
started: 2026-07-04T12:18:23Z
ended: 2026-07-05T12:05:29Z
points: 16
pr_numbers: [267, 268, 269]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/361b9d14-aed7-4c54-936b-a984b84425b7.jsonl
---

# Session 44 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 10.0 — Neon project + branches + docker CI Postgres

**Completed:**
- Pre-task: promoted main → production at v0.11.1 (91 commits, DEC-041 cutover live; Eric ran the prod `db push` beforehand). Hand-bumped v0.11.1 (patch for #266) since /retro wasn't due — CHANGELOG + tag on main.
- `docker-compose.yml` — postgres:17, bushel_dev + bushel_test, host port 5433 (muster owns 5432); `db/init/01-create-test-db.sql`
- `.github/workflows/ci.yml` — postgres:17 service + sanity step (Supabase steps stay until 10.5/10.6)
- `db/roles/mcp_readonly.sql` — applied to Neon `production` branch; login verified, writes denied; URL saved in `.envrc.production` as `MCP_READONLY_DATABASE_URL`
- Neon project via Vercel Storage integration; branches `main` (dev/preview) + `production`; Vercel env split per scope (Production → production branch, Preview → main branch; Development scope was locked in dashboard, skipped — only feeds `vercel dev`)
- `.envrc` (dev URLs) + `.envrc.production` (PROD_* URLs) filled by Eric; `.gitignore` covers `.envrc.production`

**Code review:** 1 finding fixed (documented ALTER DEFAULT PRIVILEGES role-binding assumption — must run as neondb_owner, same role as prod migrations); 2 advisory (neondb name documented, CI healthcheck cadence drift vs muster intentional)
**PR:** [#267](https://github.com/mobiustripper42/bushel/pull/267)
**Points:** 3
**Branch:** task/10.0-neon-setup
**Opened at:** 2026-07-04T12:18:23Z

## Task 2: 10.1 — Port db/migrate.ts runner + 0001_init.sql clean baseline

**Completed:**
- Pre-task: merged #267 (squash), 10.0 closed. Answered MCP-auth question: the `mcp_readonly` password lives inside `MCP_READONLY_DATABASE_URL`; actual MCP server wiring deferred (move var to `.envrc` when wanted).
- `db/migrate.ts` — muster port (byte-identical logic; bushel names, port 5433, DEC-049 explicit-arg prod form)
- `db/migrations/0001_init.sql` — clean baseline from schema dump of final Supabase state; DEC-048 deletions applied (no RLS, no public.users, no auth.users FKs; customer_sends.sent_by_user_id kept as bare uuid). Seed: 7 codes rows + both singletons. NOTE: no send_queue table exists — DEC-048's mention maps to customer_sends.sent_by_user_id.
- `package.json` — db:up/db:down/db:migrate; pg in dependencies (review fix), @types/pg + tsx devDeps
- Verified: idempotent apply on docker bushel_test; place_order smoke (create/append/replay + trigger + qty math); applied to Neon `main` (wiped + re-applied after the index-comment fix so the applied file matches the committed file)

**Code review:** 2 findings fixed (restored COMMENT ON INDEX orders_one_open_per_customer; pg → dependencies). Advisory for 10.7 runbook: pg sslmode warning on stderr during Neon connects is noise. Review confirmed baseline parity programmatically.
**PR:** [#268](https://github.com/mobiustripper42/bushel/pull/268)
**Points:** 5
**Branch:** task/10.1-migrate-runner
**Opened at:** 2026-07-05T12:30:00Z

## Task 3: 10.2 — pg data layer: swap supabase-js call sites to pg

**Completed:**
- Pre-task: merged #268 (10.1). Answered MCP-auth question (password lives in the URL; wiring deferred).
- New: `src/lib/db.ts` (Pool + type parsers pinning supabase JSON shapes), `db-types.ts`, `pg-errors.ts`, `admin/order-status.ts` (pure domain split — keeps pg out of client bundles), `admin/auth.ts` (transitional getAdminUser; 10.3 swaps the body)
- Converted: 6 query libs, 14 actions (each gained an explicit getAdminUser gate — replaces cookie-client+RLS), 3 admin pages, cron route
- Tests: helpers.ts + global-setup data seeding → pg; 13 spec files converted via 3 parallel subagents (transport swap only); supabase kept for admin session minting until 10.3
- CI: job-level DATABASE_URL → postgres service, migrate step before build, -e DATABASE_URL into the Playwright container
- Verified locally: 49 targeted Playwright tests green (desktop + mobile 375px) against app-on-docker-pg; build confirms no pg in browser bundles
- Local dev DB migrated; Neon `main` re-seeded earlier (10.1)

**Code review:** 1 finding fixed (getOpenOrder multi-row invariant guard restored). Confirmed clean: action-gate security sweep, place_order param parity, JSON-embed shape parity, pool lifecycle, test seeding. `src/lib/supabase/admin.ts` dead — 10.5 removes.
**PR:** [#269](https://github.com/mobiustripper42/bushel/pull/269)
**Points:** 8
**Branch:** task/10.2-pg-data-layer
**Opened at:** 2026-07-05T13:55:00Z

**Next Steps:**
- 10.3 (#256, 8 pts): email-code auth — muster `session.ts` port + `login_codes` table + Resend (DEC-047). **Prereq: Eric needs a Resend account + domain verification** (in 10.3's scope). Port sources: `~/muster/src/auth/session.ts`. Retires `src/lib/admin/auth.ts`'s transitional body and the Playwright storage-state dance.
- Then 10.4 (RLS/pgTAP prune), 10.5 (remove @supabase/*), 10.6 (login-code test helper — note full suite ALREADY green on pg since #269, so 10.6 is mostly the auth-helper swap), 10.7 (Neon prod cutover, now Neon-move-only since DEC-041 shipped with v0.11.1).

**Context:**
- **Pre-Phase-10 in this session:** promoted main → production at v0.11.1 (DEC-041 cutover; Eric ran prod db push first). v0.11.1 was a hand-run patch bump (no /retro due) — CHANGELOG + tag on main.
- **Two databases during transition:** Supabase = auth only (local for tests, cloud for prod OAuth); pg/Neon = all data. Tests mint admin sessions via supabase but seed data via pg — both must point at the same app DB (DATABASE_URL).
- **Vercel Preview env saga** (see memory `vercel-preview-env-gotchas`): CLI can't add unbranched Preview vars; malformed paste → ENOTFOUND; wrong Neon branch → 42P01 (prod branch is EMPTY until 10.7). Preview scope now verified working via clean `vercel deploy`.
- Neon dev branch (`main`) is seeded with preview walkthrough fixtures (Kale/Eggs/Honey + customer `preview-tester-0001`) — 10.6/10.7 may want them cleaned or kept deliberately.
- Old test helpers silently swallowed supabase errors — pg throws. One latent FK bug surfaced in CI (fixed b6a3942); expect more of this class if old branches get rebased onto pg.
- PROJECT_PLAN drift for /retro: Phase 10 sequencing text still says 10.7 covers "BOTH DEC-041 + Neon move" — DEC-041 half already shipped.
