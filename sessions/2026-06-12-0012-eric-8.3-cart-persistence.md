---
session: 38
dev: eric
slug: 8.3-cart-persistence
branch: task/8.3-cart-persistence
started: 2026-06-12T00:12:12Z
ended:
points:
pr_numbers: [216, 217]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/582f8f81-9c07-4532-8126-8a82d35c2044.jsonl
---

# Session 38 — 8.3-cart-persistence

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: product_units strictly authoritative — drop mirror columns (#212, DEC-037)

**Completed:**
- Migration `supabase/migrations/20260612120000_drop_product_unit_mirror.sql`: drops the two mirror triggers/functions (`20260526152348`), the `products_spawn_default_unit` safety net (`20260522175735`), then columns `products.unit` + `products.price_cents` — in that order. `order_items_default_unit` retained (out of scope).
- `saveInventory` (`src/actions/save-inventory.ts`) now owns the base-unit (`conversion_to_base=1.0`) row: insert-then-insert-base-unit with compensating delete on create; base-row label/price update on inline edit; `23505`→friendly row error; **fails loud** if no base row exists (review fix).
- `saveProductUnits` (`src/actions/save-product-units.ts`): folded-in fix (reject $0, matching DB `>0` CHECK) + new guard requiring exactly one `conversion_to_base=1` unit per save (review fix — protects the invariant the inline editor now assumes).
- Readers swapped off the dropped columns: `orders-queries.ts`, `OrderForm.tsx`, `customer/queries.ts`, `confirmed/page.tsx`, `inventory/page.tsx` (re-sources from base unit), `types.ts` regenerated.
- Tests: deleted `mirror_products_and_base_unit.sql`; explicit base-unit rows in `seed.sql` + 4 pgTAP fixtures; new `inventory-units-authoritative.spec.ts` + duplicate-base guard case; `global-setup.ts`/`helpers.ts` updated. pgTAP 137 pass; Playwright desktop+mobile green; build green.
- Built by a Fable agent off the full spec; @architect sized the boundary (#212 = base-unit label/price *writes*; re-designation stays #208) and bumped 3→5. #208 commented with the boundary.

**Code review:** Two related invariant gaps (silent no-op on missing base unit; no exactly-one-base guard) — both fixed in `66c732c` with a Playwright case. Other notes confirmed safe.
**PR:** [#216](https://github.com/mobiustripper42/bushel/pull/216)
**Points:** 5
**Branch:** task/8.4-product-units-authoritative
**Opened at:** 2026-06-12T03:23:06Z

**Merge gate (manual, for Eric):** run the drift query from `20260526152348` against **prod** before `supabase db push` — the column drop silently anoints `product_units` as the winner for any divergent row.

## Task 2: changeable base unit — atomic invariant-preserving rebase (#208, DEC-038)

**Completed:**
- Migration `supabase/migrations/20260612153000_set_base_unit.sql`: `set_base_unit(product, unit)` — `security invoker`, atomic. Divides every unit's `conversion_to_base` AND `products.qty_available` (stored in base units) by the new base's old conversion → new base = exactly 1.0, rest rescale; renumbers `sort_order` so the new base sorts first (= customer default). Prices untouched. Raises on cross-product/inactive/missing; no-op when already base.
- `src/actions/set-base-unit.ts`: RPC wrapper + friendly error mapping.
- `src/components/admin/units-drawer.tsx`: "Make base" pill on saved+active+non-base rows; existing `ConfirmModal`; fires `setBaseUnit` immediately (not staged into Save); disabled while drawer dirty; Escape/close paths gated on busy+modal. CSS in `src/styles/app.css` (incl. 375px flex-wrap).
- Decision recorded: "base + default display" (Eric chose — make-base also moves the unit to the customer's default via sort_order). "Base unit" (conv=1.0 anchor / qty denomination) was distinct from "default-display unit" (sort_order[0]); this unifies them on change.
- Tests: pgTAP `set_base_unit.sql` (17 assertions incl. order-footprint ratio invariance, inactive-unit rescale, scale guard); Playwright `admin-base-unit.spec.ts` (confirm flow + DB check, cancel, dirty-gating). 154 pgTAP pass; Playwright 3/3 desktop + mobile@375px; build green.
- Built by a Fable agent off the full spec.

**Code review:** Confirmed atomicity + single-base invariant + drawer gating. One real low-likelihood bug fixed in `cbe122c`: a large rescale factor could round a conversion to 0.0000 and abort against the `conversion_to_base > 0` CHECK — added a pre-check + mapped message + pgTAP case. Grant posture verified safe (no non-admin authenticated user).
**PR:** [#217](https://github.com/mobiustripper42/bushel/pull/217)
**Points:** 3
**Branch:** task/8.5-changeable-base-unit
**Opened at:** 2026-06-12T04:16:54Z

**Pre-existing follow-up (not filed):** the admin units grid's label inputs are crushed at 375px (no mobile override predates this work). This PR only stops the new pill overflowing; a proper mobile units-grid layout is its own item — candidate for OPEN_ITEMS.

**Next Steps:**

**Context:**
