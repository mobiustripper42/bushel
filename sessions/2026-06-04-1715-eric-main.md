---
session: 36
dev: eric
slug: main
branch: main
started: 2026-06-04T17:15:07Z
ended:
points:
pr_numbers: [196, 197, 198, 199]
status: open
transcript: /home/eric/.claude/projects/-home-eric-bushel/399a6d22-abf1-4048-a87d-e40977f254c3.jsonl
---

# Session 36 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Harvest & Pack Sheet — public tokenized fulfillment report

**Completed:**
- New public route `/f/[token]` (`src/app/f/[token]/page.tsx` + `not-found.tsx`, `force-dynamic`) — read-only Harvest & Pack Sheet, mirrors `/c/[token]`. Proxy lets it through unauthenticated (only `/admin` is gated).
- Admin-only token table: migration `20260604180000_fulfillment_link.sql` (singleton, RLS admin-only, **no** anon policy — deliberately NOT on `ordering_schedule`, which has an anon-read policy that would leak the token). pgTAP `fulfillment_link_rls.sql` proves anon denied / admin allowed.
- Rollup `src/lib/fulfillment/report.ts` reuses `listOrders()` — harvest list (per product, per ordered unit, folded base total via `conversion_to_base`) + per-customer slips. No new query/RLS surface.
- Token lib `src/lib/fulfillment/link.ts` — `getFulfillmentToken` + `isValidFulfillmentToken` (UUID-guarded, throws on DB error).
- "Harvest sheet" link on `/admin/orders` header (`orders/page.tsx` + `orders-page.tsx`).
- View `src/components/fulfillment/fulfillment-report.tsx` + `app.css` `fr-*` section (mobile-first + print). Design spec: `design/fulfillment-report.{jsx,css}` + `design/preview-fulfillment.html`.
- `types.ts` regenerated for `fulfillment_link` (byte-identical to generator). Playwright `tests/fulfillment-report.spec.ts`.
- **Design-first:** mockup screenshotted (screen/print/mobile) → Eric signed off → then built. Live mobile render confirmed matching the mockup.

**Code review:** @code-review — RLS design confirmed correct. Fixed in commit 2436771: UUID shape-guard before DB call (scanner-traffic parity with `/c/` middleware), throw-on-DB-error instead of misleading 404, stable `orderId` slip key. Left as documented: inert `updated_at` (awaits a future Regenerate action), cosmetic base-unit label.
**PR:** [#196](https://github.com/mobiustripper42/bushel/pull/196)
**Points:** 5
**Branch:** task/8.x-fulfillment-report
**Opened at:** 2026-06-04T18:42:00Z

## Task 2: Rename "Send Update" → "Send Texts" nav item + reorder (#188)

**Completed:**
- `src/app/(admin)/admin/layout.tsx` — NAV_ITEMS: relabel "Send Update" → "Send Texts"; moved between Customers and Orders (Inventory → Customers → Send Texts → Orders → Settings).
- `design/admin-shell.jsx` — mirrored rename + reorder (design parity).
- `tests/admin-shell.spec.ts` — new test: asserts renamed label, old-label absence in nav, and Customers → Send Texts → Orders DOM order.
- Route unchanged (`/admin/send`); active-state + unsent badge key off `href`, so they carry over (verified). Mobile drawer inherits from same NAV_ITEMS. Screenshot confirmed badge intact on renamed item.

**Code review:** @code-review — clean bill of health. Confirmed active-state/badge unaffected (href unchanged), mobile drawer consistent (single source array), no stray old labels in nav. Breadcrumb/page heading still "Send Update" by design (→ #189).
**PR:** [#197](https://github.com/mobiustripper42/bushel/pull/197)
**Points:** 1
**Branch:** task/8.x-send-texts-nav
**Opened at:** 2026-06-04T19:05:00Z

## Task 3: Send Texts page — rename, drop mode tabs, reword (#189)

**Completed:**
- `src/app/(admin)/admin/send/page.tsx` — title + metadata "Send Update" → "Send Texts"; subtitle → "Send a text to each subscribed customer."; removed the order-confirmation/pickup-reminder mode tabs + all mode branching (weekly-only). Dropped `?mode` param, MODE_LABELS/MODE_SUBTITLES/isSendMode/bodyFor.
- `src/components/admin/admin-breadcrumb.tsx` — breadcrumb `/admin/send` → "Send Texts" (folded in; was the last stale chrome label).
- `src/lib/admin/send-queue-queries.ts` — removed orphaned `getOrderConfirmationQueue`/`getPickupReminderQueue` + private `getOrderBoundQueue`; reworded the unsent-count comment. **Kept** SEND_MODES, template bodies (tested + #190/#193 reuse), record-send path.
- `tests/admin-send.spec.ts` — dropped 2 mode tests + unused `ensureOrderForCustomer`; added weekly-only assertion.
- `tests/notifications-flow.spec.ts` — removed "mode independence" test (drove the removed `?mode=order_confirmation`) + its helpers.

**Code review:** @code-review caught a real one I'd missed — `notifications-flow.spec.ts` still drove the removed `?mode=order_confirmation` view (would fail full suite). Fixed in 517cbf2 (test removed; weekly-half already covered elsewhere) + stale comment reworded. Rest clean.
**PR:** [#198](https://github.com/mobiustripper42/bushel/pull/198)
**Points:** 2
**Branch:** task/8.x-send-texts-page
**Opened at:** 2026-06-04T19:45:00Z

**Context (carry):**
- **Pre-existing mobile flake:** `admin-send.spec.ts` "intro note saves" fails first-try on WebKit (Save dirty-detection doesn't fire on Playwright `.fill()`), passes on retry → green in CI (retries:2), red locally (retries:0). Fails on `main` too. Candidate `bug` issue if anyone wants it chased.
- The Send→Orders cluster: #188 (nav, merged), #189 (this, page) done. Remaining: **#190** (Confirmed status + Orders redesign — the big one, 3pt, foundation), #191 (extract SendAction, 2), #192 (per-order action stack, 5), #193 (delivery reminder, 2).

## Task 4: "Confirmed" order status + flow (#190, DEC-035)

**Completed:**
- Migration `20260604200000_order_status_confirmed.sql` — `codes` row confirmed@2; renumber ready/picked_up/delivered→3/4/5. Data-only (no DDL → types.ts unchanged). orders.status stays app-enforced text.
- `src/lib/admin/orders-queries.ts` — `OrderStatus`/`ORDER_STATUSES` += confirmed; exported pure `isValidTransition` (4-path table) + `statusAfterConfirmSend` (no-regress rule for #192's confirm-send button).
- `src/actions/advance-order-status.ts` — imports `isValidTransition` (can't export non-async from a `"use server"` module, so the table moved to orders-queries).
- `src/components/admin/order-row.tsx` + `app.css` — confirmed branch (Confirmed pill + "Mark ready →"); `.pill-confirmed` copied from design mockup. Fixed fallthrough that rendered confirmed orders as "Delivered".
- `docs/DECISIONS.md` — **DEC-035** (confirmed). Issue said DEC-034 but that's already the mobile-admin number (referenced in PROJECT_PLAN/RETROS/app.css/playwright.config, never formally written) — user chose DEC-035; added a reserved DEC-034 stub line for contiguity.
- Tests: pgTAP `order_status_codes` (ordinals); unit `order-status-transitions` (full table + no-regress); `admin-orders` integration (confirmed pill + advance); `SeedOrderInput` += confirmed.

**Code review:** @code-review clean. Verified no client-bundle leak (order-row imports OrderStatus type-only; isValidTransition only in the "use server" action), exhaustive confirmed switch, re-run-safe migration. Noted pre-existing: `codes.order_status` rows are reference data nothing reads (hyphen `orders.status` vs underscore `codes.code` never join) — not this PR's fix.
**PR:** [#199](https://github.com/mobiustripper42/bushel/pull/199)
**Points:** 3
**Branch:** task/8.x-confirmed-status
**Opened at:** 2026-06-04T20:25:00Z

**Next Steps:**
- **DEC-035 migration is local-only** — `supabase db push` to dev/preview, then prod, before/with merge. (Two migrations now pending prod: fulfillment_link #195 + this. Push both.)
- Remaining Orders-redesign cluster, all build on DEC-035 confirmed status: **#191** (extract shared SendAction component, 2), **#192** (per-order action stack Confirm/Reminder/Mark in expanded detail — wires `statusAfterConfirmSend` to a real confirm-send button, 5), **#193** (delivery reminder send mode + template, 2). — add "Confirmed" order status + flow; foundation for the Orders redesign (#191/#192/#193 build on it). The big one in the cluster (3pt, likely a migration on the order_status codes + the order-detail UI).
- **Migration is local-only.** Before merge/deploy: `supabase db push` to dev/preview (vet), then prod via link→push→relink. Without it, `/admin/orders` will 500 (getFulfillmentToken) and `/f/` can't validate.
- Optional follow-on: `weekOfLabel()` is Sunday-anchored ("Week of May 31") while orders bucket on `weekOfMondayNY()` (Jun 1) — align the report header label to Monday if the mismatch bothers anyone. Pre-existing helper quirk.
- Idea PR #194 (the source doc) is still open — decide whether to merge the doc, close it, or leave it.

**Context:**
- `.env.local` points at **local** Supabase (per standing preference) — dev server + Playwright run against local, which now has the migration. That's why the spec passes without a cloud push.
- Local Supabase was down at session start; `supabase stop && supabase start` revived it before `db reset`.
- Design mockups in `design/` load React/Babel from unpkg via a `preview-*.html` harness and fetch the `.jsx` over HTTP — screenshot by serving `design/` locally and driving Playwright (throwaway script, not committed).
