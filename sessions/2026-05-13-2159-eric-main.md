---
session: 18
dev: eric
slug: main
branch: task/audit-drift-cleanup
started: 2026-05-13T21:59:50Z
ended: 2026-05-14T02:12:45Z
wall_clock: 4.25
dev_time: 2.67
review_time: 0.5
duration: 2.67
points: 0
status: closed
pr_number: 68
pr_url: https://github.com/mobiustripper42/bushel/pull/68
pr_opened_at: 2026-05-14T00:40:01Z
transcript: C:\Users\eric\.claude\projects\C--Users-eric-OneDrive-Documents-GitHub-bushel\f56c821c-e11d-465b-8b29-258c0536206e.jsonl
---

# Session 18 — main

**Task:** Off-plan consistency audit — surface drift between docs/code/mockups, fix the cheap stuff, file the rest. PR #68 (no closing issues; cleanup work, doesn't count toward phase velocity).

**Completed:**
- **PR #68 merged** — 12 files, +381/−327. Doc + mockup + small-bug cleanup; no behavior change in shipped pages beyond two save-inventory bound fixes.
- **Five-agent parallel audit** ran first — DECISIONS↔code, schema↔types↔code, SPEC/PLAN↔reality, CLAUDE.md↔repo, /design/↔shipped. Findings consolidated into a single severity-ranked report (10 High / 9 Medium / 8 Low). All but a handful addressed in PR #68; deferred items filed as #66/#67.
- **`docs/SPEC.md` + `docs/USER_STORIES.md` filled** — first-pass port from `docs/review/spec-for-annabel.html` + DECISIONS + PROJECT_PLAN. Both flagged "pending review" at the top. CLAUDE.md was citing them as authoritative while they were still empty seeds templates — biggest load-bearing drift the audit caught.
- **`CLAUDE.md` cleaned** — dropped four wrong DEC cites (007/008/009 in CLAUDE.md pointed at seeds-template concepts; bushel's DEC-007/008/009 are Pricing/Fulfillment/Delivery-zone). Bumped DEC range to ...DEC-032. Added SCHEMA.md to Key Docs. Clarified docs/AGENTS.md is canonical (root AGENTS.md is a Next.js IDE-rules stub). Added @sync-config + @tape-reader to the Agents table. Noted prod-write wrapper is a no-op without `.claude/prod-supabase-refs`. Removed BRAND.md TODO line (file is filled). VersionTag move-to-login → explicit Phase 6.1 task.
- **`docs/DECISIONS.md`** — DEC-018 workers count corrected (`workers: 1` everywhere, not 4-local-1-CI). DEC-020 supersede header now mentions DEC-028's column drop.
- **`docs/SCHEMA.md`** — customers table updated (priority + send_weekly_link added, notification_preference removed, phone NOT NULL); ordering_schedule.is_singleton documented; trade-offs section refreshed.
- **`docs/PROJECT_PLAN.md`** — DEC range bumped; pickup_windows in 1.1b struck through.
- **`design/admin-customers.jsx` + `.css`** — Notify column → Priority column per Phase 3.0 (DEC-026 + DEC-028); drawer adds business_name + Priority, drops notification-preference radios. Mockup now matches shipped customers-page.tsx.
- **`src/actions/save-inventory.ts`** — `price_cents` validation `>=0` → `>0` (matches DB CHECK; was silently passing 0 through to opaque CHECK violation). `qty_available` no longer rejects negatives — DEC-012 optimistic oversell intentionally allows admin to record an oversold state by direct entry.
- **`src/styles/customer.css` deleted** — last area-file holdout per CLAUDE.md UI/Brand rule. Unique classes folded into `app.css` under labeled customer-pages sections; duplicate `.btn`/`.eyebrow`/`.mono` definitions discarded (app.css versions were richer). `src/app/c/layout.tsx` import removed.
- **#66 + #67 filed** as `phase:3` polish — admin shell rebuild (top bar + sidebar footer + nav badges) and pgTAP coverage for DEC-029/030 columns. Re-labeled from `phase:6` after user clarification mid-session.

**In Progress:** Nothing.

**Blocked:** Vercel prod env vars verification still pending off-session (carry-over from sessions 16 + 17, not touched this session).

**Next Steps:**
1. **Resume Phase 3.3 (#48, 3pts)** — `/c/[token]` route + cookie + token-rotation invalidation. Original session-17 next step; session 18 was an off-plan diversion. Auth-guard generalization (#63) decision still pending — do before 3.3 OR co-design.
2. **Issue #66 (admin shell rebuild, 5pts)** — sized into Phase 3 polish but doesn't have to land before 3.3. Could batch with 3.3 if it's painful to demo customer-side without the breadcrumb/week-pill admin chrome.
3. **Issue #67 (pgTAP DEC-029/030 coverage, 2pts)** — small; can be opportunistically rolled into the next migration-touching PR.
4. **SPEC.md + USER_STORIES.md review** — both first-pass drafts; review at leisure and amend. Removing the "pending review" banner is the signal they're trusted.
5. Verify Vercel prod env vars (carry-over) + Preview env vars (#60).

**Context:**
- **Five-agent parallel audit pattern worked well.** ~80s wall-clock per agent, ran concurrently, returned tight 400-word reports. Each took a clear axis (DECISIONS, schema, plan, CLAUDE.md, design) and the consolidated severity-grouped report had no duplicate findings. Worth reusing — `/audit` skill candidate if this becomes recurrent.
- **CLAUDE.md DEC mis-cites came from copying seeds-template wording into a project that had its own (different) DECISIONS.md numbering.** Going forward, never reference DECISIONS by number in CLAUDE.md unless verifying the citation matches. `/pull-seeds` might want a check for DEC-N references in the wording it propagates.
- **save-inventory.ts bound mismatch was a real lurking bug** — the audit caught it because cross-referencing the migration's `check (price_cents > 0)` against the action's `>= 0` was the explicit task. A test-the-user code review of the action alone wouldn't have found it; the action validates and returns errors cleanly, just with wrong bounds. Reinforces DEC-023's "test the user, not the function" — but also that doc-vs-code audits catch a different class of bug than reviews do.
- **Mockup-vs-shipped audit found one Medium drift on a shipped page** (admin-customers Notify→Priority), already-known scope shift but mockup never caught up. Worth re-running this audit at each phase boundary as a discipline. The shell+login drifts are real but are correctly tracked as Phase 3.x / 6.x polish.
- **No /kill-this this session.** PR was opened directly via `gh pr create` mid-flow; user merged from mobile then asked /its-dead to clean up. Time fields back-filled from PR createdAt. `/its-dead` recovered fine — worth noting that the OPEN→MERGED path with no /kill-this is fully supported by the skill.

**Code Review:** Skipped — no /kill-this run, so no @code-review pass on the diff. The code change footprint is small (one file, two lines of validation logic; CSS file delete + fold; one mockup data-shape update). PR #68's CI passed.
