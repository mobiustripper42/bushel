---
session: 16
dev: eric
slug: main
branch: main
started: 2026-05-12T10:36:09Z
ended: 2026-05-13T00:45:45Z
duration: 4.17
points: 3
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/2facc2e8-4394-4f17-acc3-88d176fe5d88.jsonl
---

# Session 16 — main

**Task:** Phase 3.1 redo — rebuild `/admin/customers` against `design/admin-customers.jsx` using the app.css primitives from Phase 2.1. Prior orphan attempt (PR #55) shipped against the wrong spec; this was the clean rebuild. Plus end-of-session: AC change to phone-required, infra task created for mobile preview review, deactivated-customer issue filed.

**Completed:**
- **PR #59 merged** — closes #46. 6 customer specs + 12 regression specs green.
- **Drawer-based CRUD UI** — `customer-drawer.tsx` (form + OR-validation-turned-phone-required + ESC-to-close), `customers-page.tsx` (table + drawer state + inline subscribe toggle), `app/(admin)/admin/customers/page.tsx` (server fetch by priority, name).
- **Three server actions** — `save-customer.ts` (validation + placeholder UUID token), `deactivate-customer.ts` (soft delete), `toggle-subscribed.ts` (inline send_weekly_link).
- **CSS folded into `app.css`** — `admin-customers.css` created then immediately deleted after the user flagged it. All page CSS (drawer primitives, customer-table classes) now in `app.css` under labeled sections. Stale CSS-organization memory deleted; rule codified in CLAUDE.md §UI / Brand + redundant memory pointer so it sticks every session.
- **Global-setup hardening** — TEST_CUSTOMERS upserted alongside TEST_PRODUCTS so admin-prepopulate.spec.ts and admin-customers.spec.ts are both self-contained against remote DBs. beforeEach in customers spec resets fields for order-independence.
- **Code review findings addressed pre-merge** — try/catch on server-action calls (matches inventory baseline), priority held as string (no empty→0 coercion footgun), placeholder tokens use full UUID until 3.2 lands the short-token generator, inline error banner for failed toggle with `router.refresh()` resync.
- **Phone now required, email optional (AC change late in session)** — server validation rejects empty phone; drawer label reads "Phone (required)"; field-help text explains DEC-026 rationale. Error banner moved to top of drawer body so it's visible immediately on save failure.
- **Two follow-up issues filed** — #60 (preview.baybranchfarm.com + mobile OAuth infra), #61 (show deactivated customers toggle).
- **Branched off main, force-pushed to replace orphan `task/3.1-admin-customer-crud` on origin** — session 15 had flagged the orphan for deletion. Local archive: `archive/3.1-old-attempt`.

**In Progress:** Nothing.

**Blocked:** Nothing — but **prod is currently 500ing** (`order.baybranchfarm.com` AND `bushel.vercel.app`). Not blocking 3.2 work, but real. User is going to verify/fix Vercel env vars off-session.

**Next Steps (cold-start ready):**
1. **VERIFY PROD ENV VARS** in Vercel — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` for Production environment. Compare exactly to `.env.local`. Almost certainly stale from the bushel/sailbook account split. Redeploy after fixing. Hit `https://order.baybranchfarm.com/` — should return 200.
2. **Same three vars for Preview environment** too (issue #60).
3. Delete `archive/3.1-old-attempt` local branch (and any older orphans from session 15: `task/2.1-inventory-editor`, `task/2.2-prepopulate-from-last-week`, `task/2.2-prepopulate-clean`, `task/3.0-customer-priority-column`).
4. **Phase 3.2 (#47, 5pts)** — crypto token generation + mandatory regenerate-before-send. Wire the disabled Regenerate button + confirm modal that's already rendered in `customers-page.tsx`. Design uses `k4f8x2-w7s`-style slug (6+3 with hyphen).
5. **Phase 3.3 (#48, 3pts)** — `/c/[token]` route validates token + sets cookie. Customer-side starts here.
6. Decide if #60 (mobile preview infra) lands before or after 3.2 — user wants it "next task or very soon".

**Context:**
- **CSS one-file rule is now in CLAUDE.md §UI / Brand** and re-memory'd. Per-page CSS files are bad practice on this project; if anyone (including user) proposes one next session, push back. Session 15 + 16 both re-litigated this; CLAUDE.md is now load-bearing.
- **Vercel env-var sync gap** — when bushel was split from sailbook for billing, Vercel env vars were not re-synced to the new Supabase project. Prod has been silently 500ing since. Issue #60 captures the fix + documentation update so this doesn't recur.
- **Design tension carry-forward** — design's "Notification preference" radio (sms/email/both) is stale; schema dropped `notification_preference` for `send_weekly_link` boolean. Resolution this session: single Subscribed switch. Same tension will recur for any design file written pre-2.0a. Read both design AND schema before trusting a mockup.
- **Playwright `getByLabel` requires implicit label-for-control association.** Original Field component had `<label>` as a sibling to the input — selector couldn't resolve. Fix: wrap the whole field in `<label className="field">` with span children. Pattern to reuse in future drawer/form components.
- **`getByRole("alert")` matches Next.js's route-announcer div.** Always scope to a container (e.g. `drawer(page).getByRole("alert")`) when asserting on a custom alert.
- **Existing remote PR-on-orphan-branch:** the abandoned orphan had a merged PR #55. Force-push replaced the branch but the PR record persists on GitHub — opened #59 fresh. Pattern to watch when rebuilding any task whose prior PR was merged.
- **Token strategy carry-forward:** `tmp-<full-uuid>` placeholder is safe enough to be reachable but cosmetically ugly. 3.2's design-spec slug (`k4f8x2-w7s`) replaces it; existing placeholder rows will need regenerate-before-send (already a 3.2 AC).
- **Migrations are in sync remote-side** — `supabase migration list` confirmed all 8 applied. The session-15 "pending migrations" carry-forward is resolved.
- **Per-field inline form validation** is a stretch follow-up (user mentioned wanting "something right on the text box" eventually). Not ticketed yet — surface at planning if relevant.

**Code Review:**
- customer-drawer.tsx: missing try/catch around server actions → **fixed** (network failures surface inline).
- customer-drawer.tsx: `parseInt(value) || 0` coerced empty priority to 0 → **fixed** (held as string, parsed at save).
- save-customer.ts: 32-bit placeholder token → **upgraded** to full UUID.
- customers-page.tsx: toggle swallowed errors silently → **fixed** (inline alert banner + router.refresh resync).
- Deferred (flagged but appropriate for V1): row stopPropagation pattern, error-page retry, clipboard silent-fail, token collision on insert (3.2 owns it).
