---
session: 28
dev: eric
slug: main
branch: main
started: 2026-05-18T17:30:39Z
ended: 2026-05-18T19:07:49Z
points: 0
pr_numbers: [142]
status: closed
transcript: C:\Users\eric\.claude\projects\C--Users-eric-OneDrive-Documents-GitHub-bushel\f56c821c-e11d-465b-8b29-258c0536206e.jsonl
---

# Session 28 — main

<!-- Baseline sweep — shipped without per-task /kill-this. Two PRs merged from this session: seeds#51 (DEC-016 + generic ui-reviewer shell) and bushel#142 (ui-context.md split + sync-config indent + retro DEC-015 adoption). Points = 0 by DEC-013 rule since no Task blocks recorded; the work is real but lives in Context. -->

**Next Steps:**
- Fire the nightly sync and confirm both seeds and bushel come back with zero PRs. If it does generate a PR for bushel, the @sync-config classification of `ui-reviewer.md`'s brand-words description line is the most likely culprit (worst case = still skipped Both-modified, just for a different reason).
- Sailbook + crewbook need the same DEC-016 split — extract their project-specific design content to `.claude/ui-context.md`, slim their `ui-reviewer.md` to match the new seeds shell. Bushel#142 is the migration template.
- bushel's `docs/DECISIONS.md` already has its own DEC-016 (Wave invoicing). Seeds DEC-016 (ui-reviewer split) lives in seeds only and doesn't collide because the files are independent — but future projects seeded from v3+ should start their project decisions at DEC-017.

**Context:**
- **DEC-016 (seeds) — ui-reviewer split.** The agent file used to be both generic shell and project brand content, which made it permanently Both-modified for every webapp project. Now: `dev/claude/agents/ui-reviewer.md` is a sync-clean generic shell that reads `.claude/ui-context.md` at runtime; each project keeps its design system in that context file. Bushel's brand tokens, surfaces, typography, components, and What-to-Check checklist all moved to `.claude/ui-context.md`. The agent file is now near-identical to the seeds template (only project-name substitutions differ).
- **sync-config.md indent fix.** Bushel had 2-space indent on the "One commit per source repo..." line vs seeds' 4-space (continuation under bullet list). PR #141 the prior day applied other sync-config.md changes from seeds but didn't touch that line — it had been silently Both-modified for weeks. Now identical.
- **retro DEC-015 adopted.** Bushel's `/retro` skill now uses per-PR dev/review windows instead of the older `max(pr.createdAt)` single-seam boundary. The "Bushel Phase 3 surfaced the artifact: 0.15 h/pt dev vs 0.35 h/pt active" caveat is gone — the per-PR formula is the fix. Historical phase retros stay as written; their notes already flag the artifact.
- **Seeds orphan branches.** Two `claude/*` branches showed in local tracking refs at session start (`extract-captainslog-repo-4O6ln`, `organize-project-structure-GGPir`) but were already gone from the remote — stale local refs only. Confirmed clean via `gh api repos/.../branches`.
- **Nightly sync mechanism.** Lives at `seeds/dev/claude/routines/nightly-sync.md` — an Anthropic Routine (claude.ai), not a GitHub Action. Runs `@sync-config` in both directions per repo in the chip-area scope. Excludes seeds itself as a target. Pro plan cap = 5 Routine runs/day across all configured Routines.
- **No /kill-this called this session.** Baseline-sweep work didn't follow the per-task PR flow — both PRs were opened directly via `gh pr create`. Points = 0 per DEC-013 (no Task blocks → 0). If retro math at phase close needs to credit the cleanup work, it'll need to be backfilled manually in Notes.
