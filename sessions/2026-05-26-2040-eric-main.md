---
session: 33
dev: eric
slug: main
branch: main
started: 2026-05-26T20:40:29Z
ended: 2026-05-26T22:52:32Z
points: 3
pr_numbers: [177, 178]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-bushel/47c9754b-bafa-41ab-b0a1-38e1ae989507.jsonl
---

# Session 33 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Inventory qty input — accept fractional values

**Completed:**
- `src/components/admin/inventory-row.tsx` — qty input flipped from `parseInt + inputMode="numeric"` to the same decimal draft pattern the units drawer already uses (`parseFloat + inputMode="decimal"`), rounds to 2dp on save to match the `numeric(10,2)` DB column. Without this, every multi-unit order that left a fractional remainder (e.g. half-pound unit consumes 0.5 base lb, qty_available becomes `2.5`) silently lost the fraction on Annabel's next save.
- `src/actions/save-inventory.ts` — server validator relaxed from `Number.isInteger` to `Number.isFinite`; matching 2dp rounding on store as defense in depth. The pre-existing DEC-012 comment ("qty allows negatives — admin must be able to record oversold by direct entry") was aspirational; client validator was stricter than server. Code review caught this asymmetry and `7238ce0` resolved by dropping the client's `v >= 0` guard on qty to match. Price + conv keep `>= 0` (no semantic meaning for negatives there).
- `design/admin-inventory.jsx` — mockup mirror (design folder is authoritative).
- `tests/admin-inventory.spec.ts` — new case: fill "2.5" → Save → reload → still "2.5".

**Code review:** One real finding addressed in `7238ce0` — client `v >= 0` qty guard contradicted server's DEC-012 negative-allowance. Dropped on the client to align.
**PR:** [#177](https://github.com/mobiustripper42/bushel/pull/177)
**Points:** 1
**Branch:** task/7.x-inventory-qty-decimals
**Opened at:** 2026-05-26T21:07:41Z

## Task 2: Order-detail oversold gate + Sold-this-week column on /admin/inventory

**Completed:**
- `src/components/admin/order-detail.tsx` — gated per-line oversold check on `order.needsReconciliation`. The prior `baseRequested > i.qtyAvailable` compared against the current (post-decrement) product qty read at list-load time, so once a product hit zero every prior order touching it lit up "oversold" — even orders that fit at placement. `needs_reconciliation` is the order-time truth (set by place_order only when this order's decrement pushed qty_available negative), so it's the right gate.
- `src/app/(admin)/admin/inventory/page.tsx` — 6th parallel query selects `order_items` joined to `orders!inner` filtered to current `week_of`, plus `product_units(conversion_to_base)`. Aggregates client-side (multi-unit join exceeds PostgREST's `count: exact`) into `Record<productId, sum(qty * conv)>`. Error short-circuit extended to cover the new query. Inline comment notes that an `orders.status` filter would land here if cancellation grows (DEC-012 reconciliation could spawn one).
- `src/components/admin/inventory-editor.tsx` + `inventory-row.tsx` — new `soldByProductId` / `soldThisWeek` prop chain; new `<th>Sold</th>` between Qty and Available; cell renders via local `fmtSold(n)` (em-dash on zero, trimmed-trailing-zero fixed-2dp otherwise — mirrors order-detail.tsx's `fmtQty` with a "extract to shared if a third consumer appears" comment).
- `src/styles/app.css` — `.field-sold { display: block; padding; color }` so a read-only `<span>` inherits `field-num`'s right-align inside the table cell. `.field-sold.is-zero` dims to `--ink-400` so untouched rows recede.
- `design/admin-inventory.{jsx,css}` — mockup mirror; extracted `fmtSold` helper after code review caught the inline formatter would have produced "10." (trailing dot) for `10.00` inputs.
- `tests/admin-orders.spec.ts` — new "oversold badge gates on order.needsReconciliation" case: seeds two kale orders against qty=0 kale (one flagged, one not), asserts only the flagged order shows `li.is-oversold` + `.callout-warn`. try/finally restores kale's seed qty.
- `tests/admin-inventory.spec.ts` — new "Sold column renders this week's base-unit total per product" case: seeds two orders, asserts kale=3 (2+1), honey=1, eggs em-dash + `is-zero` class. try/finally clears orders for the week.

**Code review:** One latent mockup typo + one comment addressed in `ecce85c` — extracted design-side `fmtSold` to match shipped code (inline version had the "10." trailing-dot bug); added a note on the `order_items` query for future cancellation filtering. Everything else clean — server-component error gate covers the new query, no `any`, no per-page CSS, no RLS surface change.
**PR:** [#178](https://github.com/mobiustripper42/bushel/pull/178)
**Points:** 2
**Branch:** task/7.x-order-oversold-and-sold-column (stacked on #177; GitHub will retarget to main once #177 merges)
**Opened at:** 2026-05-26T22:47:29Z

**Next Steps:**
- `/admin/inventory` mobile redesign — Task 2 ships a new Sold column sized for desktop; mobile-card layout needs a pass to figure out where Sold belongs at 375px. User mentioned this explicitly during Task 2 planning.

**Context:**
- Both PRs merged before close: #177 (Task 1, qty decimals) and #178 (Task 2, oversold gate + Sold column).
- Stale `next start` from earlier in the session lingered on port 3001 across Task A → Task B builds. Killed via PID (`ss -tlnp | grep 3001` to find, then `kill -9`) after `lsof -ti:3001` came up empty but EADDRINUSE persisted. Sometimes lsof misses listeners owned by another shell; `ss` is the fallback. Worth noting next session if the same pattern repeats.
